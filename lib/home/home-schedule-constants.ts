/** Initial Home schedule fetch — covers virtual strip (~29 cols) plus one extend chunk. */
export const HOME_SCHEDULE_INITIAL_WINDOW_PAST_DAYS = 30;
export const HOME_SCHEDULE_INITIAL_WINDOW_FUTURE_DAYS = 30;
