# iOS UX/UI iteration — 2026-09-22

## Execution scope

- Goal: autonomously simplify the native app's interactions and keep a consistent, restrained visual language. UX takes priority. Log genuine blockers and continue other work.
- Deadline: 2026-09-23 08:00 Europe/Berlin (06:00 UTC), or available token exhaustion, whichever occurs first. Do not redeem usage resets.
- Worktree: `/Users/baichu/Desktop/项目/app开发项目/Sideseat-ios-uxui`.
- Branch: `codex/ios-uxui-20260922`.
- Source: `/Users/baichu/Desktop/项目/app开发项目/Sideseat`, originally on `feat/native-ui-design-system` at `3fba50d`.
- Baseline commit `48785fb` preserves the existing uncommitted native changes and current product/design documentation. It is inherited work, not a new UX result from this goal.
- Source repository and concurrent web work stay untouched. No production writes, deployment or TestFlight upload.
- Dedicated simulator: `AEBD816E-53A7-4012-B76A-FEF82CF02A6B` (SideSeat UX QA, iPhone 17 Pro, iOS 26.5).
- Build cache: `/tmp/sideseat-ios-uxui-derived`.
- Heartbeat: `sideseat-ios-uxui` (this task only; the existing web heartbeat is separate).

## Verification and findings

- Baseline screenshot matrix started with `SideSeat-Development`, fixture-based authenticated sessions, and `VisualQAScreenshotUITests/testCaptureCurrentAppearanceMatrix`.
- Evidence: `/tmp/sideseat-ios-uxui-baseline.log` and `/tmp/sideseat-ios-uxui-baseline.xcresult`.
- Baseline native compilation succeeded (the inherited unused `payload` warning is removed by the Messages change). First UI run did not execute: new simulator's app/container service stalled; cancelled after ~3 minutes in the test-start phase, rebooted only the dedicated simulator, and split subsequent build-for-testing and test-without-building. The interrupted `.xcresult` is incomplete and is not passing test evidence.

## Verified first iteration

- Chat search opens ready to type, supports interactive keyboard dismissal and uses the shared empty-list treatment.
- Messages keeps empty content in its refreshable list, shows refresh failures even when the cached list is empty, exposes Retry, and links the empty state directly to Together. No-results search can be cleared in one tap.
- Explore search has a 44pt clear action; category filters expose visual/checkmark/VoiceOver selection, and no-results offers one action to clear both filters. Existing tokens and navigation are preserved.
- Me's identity card switches to a full-width vertical layout at accessibility text sizes; management-row values move below their labels. This fixes the observed German profile screenshot where the avatar squeezed the name and school into a narrow, heavily wrapped column.
- Shared press feedback respects Reduce Motion while keeping opacity feedback.
- Added focused fixture-driven UI journeys for these paths; English, Chinese and German strings validated with `plutil`, and `git diff --check` passes.

## Regression evidence

- First complete run: `/tmp/sideseat-ios-uxui-search-tests.xcresult`. Five tests ran: the light appearance matrix passed, four new interaction tests failed. The failures exposed child accessibility identifiers being replaced by container identifiers and a chat-result row whose whitespace did not receive taps. Fixed container semantics and label hit regions; do not count this run as passing interaction evidence.
- Second run: `/tmp/sideseat-ios-uxui-regression-r2.xcresult`. Only three of seven requested tests executed: existing largest-text Me and Calendar search passed, chat search still failed. Installed/built binary hashes matched. Missing test enumeration was resolved by explicitly using the generated `.xctestrun` file; all seven tests were found in `/tmp/sideseat-ios-uxui-test-enumeration.json`.
- Third run: `/tmp/sideseat-ios-uxui-regression-r3.xcresult`. Seven tests ran, four passed (chat search, inbox search recovery, Calendar search, existing largest-text Me). Fixed invalid test assumptions: short-name intrinsic width, a 44pt floating-point comparison, and scrolling an offscreen horizontal filter before revealing it vertically.
- Fourth run: `/tmp/sideseat-ios-uxui-regression-r4.xcresult`. Both Me accessibility journeys and the three-language Explore recovery journey passed. All 30 theme/contrast checks and five localization checks passed. Empty Messages reached its CTA, but exposed a real missing route: `/together` was not recognized. Added the canonical path alongside the existing `/discover` alias and covered relative, custom-scheme and HTTPS forms.
- Screenshot review of fourth run found German “Clear filters” truncated by the shared secondary button's one-line limit. Removed that limit to allow the action to grow vertically; preserved the minimum 44pt target and existing style. Me's accessibility layout is now visually confirmed, including complete German management labels after removing redundant category icons at those sizes.
- Fifth run: `/tmp/sideseat-ios-uxui-regression-r5.xcresult`. The complete empty Messages → Together journey and all 19 Navigation tests passed. Explore's test tried to scroll before its first control existed, then scrolled past it; corrected the test to await the control and scroll initially only for the largest-text scenario.
- Sixth run: `/tmp/sideseat-ios-uxui-regression-r6.xcresult`. Both tests passed: three-language filter recovery and retaining a search when switching Together sections. Reviewed the German screenshot: the complete reset label now wraps without ellipsis.
- First iteration acceptance: eight focused UI journeys pass across the runs above, plus the light root-screen screenshot matrix. All 54 relevant unit checks pass (30 theme/contrast, five localization, 19 navigation). Earlier failed bundles are retained as diagnosis evidence and are not counted as passing runs. No production account was changed.
- Checkpoint commit: `5b78f6b` — search recovery and large-text navigation.

## Verified authentication, Settings and reply iteration

- Login now appears immediately instead of fading/sliding its controls in separate stages. Large-text secondary actions stack to preserve their labels.
- Password reset starts with the valid email entered on login; usernames remain out of the email field. Canceling returns to the existing login draft. No new network action or automatic send is introduced.
- Signup and password reset show the existing eight-character requirement beside the new password field before submission.
- Replaced 41 English fallback values in German authentication controls, validation and recovery messages; retained localization keys and placeholders.
- `/tmp/sideseat-ios-uxui-auth-r1.xcresult`: all 16 localization/auth transport checks passed; existing failed-login and signed-out launch journeys passed. New UI journeys failed because XCTest considered offscreen buttons behind the keyboard hittable. Corrected the test gesture to reveal controls inside the actual visible content before tapping.
- `/tmp/sideseat-ios-uxui-auth-r2.xcresult`: German signup and the five-tab dark screenshot matrix passed. The English password reset entry still hit the keyboard; tightened the UI gesture bounds. Screenshots exposed a nearly invisible login tagline in dark mode; it now uses the shared strong secondary text token.
- `/tmp/sideseat-ios-uxui-auth-and-flows-r3.xcresult`: five of seven UI journeys passed: both email/username password reset cases including cancel/draft preservation, German signup, incorrect-password preservation, one-save intention creation, and the complete Plan counterproposal/outcome flow in light and dark modes. Two Calendar assertions failed and are being investigated separately.
- `/tmp/sideseat-ios-uxui-calendar-settings-r4.xcresult`: manual Calendar creation passed after removing a stale expectation for an optional smart-fill feature disabled in that fixture. Settings language selection passed, but its screenshot exposed severely squeezed language/notification rows at accessibility sizes. Calendar's drag-dismiss assertion still failed; the gesture began inside the nested Notes input. Testing the form gutter next.
- Screenshot inspection exposed a clipped German signup toolbar title, overly long degree labels, and an unlocalized integer semester label. Shortened the contextual create action while retaining its full accessibility label, use concise German degree names, show the degree menu at accessibility sizes, and add the missing integer-format translation in all three languages.
- The root-only dynamic-type test override did not enlarge presented auth sheets. New auth verification now also sets the actual UIKit preferred content-size category; earlier reset screenshots must not be counted as largest-sheet coverage. Subsequent verification is recorded below.
- Expanded the existing root-screen screenshot matrix to include the fifth destination, Plans.

- `/tmp/sideseat-ios-uxui-auth-settings-reply-r7.xcresult`: five localization checks, largest-German Settings switching, and password reset (both cases) passed. Reply cancel had inherited its parent screen identifier; added explicit accessibility containment to direct/community chat roots. Signup's large-text semester test still failed; final screenshots also revealed an oversized password-visibility icon and clipped toolbar title. Capped only decorative icon scaling, shortened the large-text navigation title, and removed the school heading's decorative icon at accessibility sizes.
- `/tmp/sideseat-ios-uxui-auth-reply-r8.xcresult`: four of five UI journeys passed: chat search and return, compact reply with a 44pt cancel target and preserved draft, incorrect-password draft preservation, and the full five-tab dark screenshot matrix. Signup's final semester assertion failed again, while its screenshot confirms the corrected icon and title. Its execution log reports older gesture coordinates and source-line assertions despite updated source; built and installed runner SHA-256 hashes match (`22f04e61…`). Preserving the existing test build/runner as `/tmp/sideseat-ios-uxui-uitest-build-r8` and `/tmp/sideseat-ios-uxui-uitest-runner-r8.app`, then rebuilding and reinstalling only the dedicated UI-test runner to resolve this evidence inconsistency. No app data or source checkout was deleted.

- `/tmp/sideseat-ios-uxui-signup-r9.xcresult`: after rebuilding and reinstalling the UI-test runner, the existing test passed both standard and actual accessibility5 scenarios, including incrementing the semester from 1 to 2. The rebuilt runner hash remained unchanged; the precise cause of the earlier execution mismatch is unconfirmed. Screenshots confirm the complete large-text title, restrained visibility icon, readable password guidance and functional degree/semester controls.
- Acceptance: password reset with valid-email reuse/username exclusion/cancel preservation; German signup with standard and real largest text; failed-password preservation; adaptive Settings language selection; and compact reply cancellation preserving the typed draft all pass. Chat search/return and the five-tab dark matrix pass after the changes. The existing 16 in-memory auth/localization checks passed in r1; five localization checks passed again in r7. Manual Calendar form, one-save intentions and Plan proposal/outcome flows were also checked as recorded above. Calendar scroll-dismiss remains a separate inherited failure, not a passing claim.

- Checkpoint commit: `6374155` — authentication, readable controls and reply draft preservation.

## Verified Plans recovery and Calendar connections iteration

- When the account has no Plans in any section, its empty state now offers the existing “Open Together” action. This matches Messages recovery and gives a concrete next step without adding a new creation path or skipping mutual interest and conversation. Existing-section empty states with Plans elsewhere retain their focused explanations.
- Extended the existing empty/loading/error Plans journey to verify the one-tap route from empty Plans to Together. No new fixture or API behavior is required.
- `/tmp/sideseat-ios-uxui-plans-connections-r10.xcresult`: both requested tests passed. Plans empty → Together plus loading/error recovery are verified. Calendar connections passed in German, but screenshot inspection shows its presented sheet still used standard text; the root-only override does not establish largest-sheet coverage. Corrected this test to set UIKit's actual largest preferred content-size category and capture the header separately before scrolling.
- `/tmp/sideseat-ios-uxui-profile-r11.xcresult`: both existing profile journeys passed: tapping labels, keyboard Next/Previous/Done, saving changed values, and protecting unsaved changes. Added explicit Chinese launch language to these tests because their controls use Chinese labels; persisted language from other tests must not determine their outcome.
- `/tmp/sideseat-ios-uxui-connections-r12.xcresult`: the corrected largest-text test passed its old reachability assertions, but screenshots revealed severe layout failures: the fixed-width German read-only badge squeezed the Apple Calendar title out of the viewport, while large icons forced action labels into narrow columns. This demonstrates why pass status alone was insufficient.
- Calendar connections now stacks its header at accessibility sizes, lets the read-only badge wrap, removes decorative file-action icons at those sizes, caps only remaining decorative icon scaling, and reuses the existing product primary button for adding Apple Calendar. No subscription or transfer behavior changed.
- `/tmp/sideseat-ios-uxui-connections-r13.xcresult`: both real largest-German and standard-Chinese dark journeys passed. Added assertions that the enlarged Apple Calendar heading is visible and inside the page. Reviewed both screenshots: the title is readable, the complete primary-action label wraps into three lines instead of narrow fragments, and import/export both fit naturally on two lines. Standard layout remains compact.
- `/tmp/sideseat-ios-uxui-calendar-action-r14.xcresult`: the existing add-connection → confirmation → revoke-prompt journey passed. Its DEBUG fixture returns before opening Apple Calendar and writes only local fixture state; no real subscription was created.
- Acceptance: Plans recovery and empty/loading/error states, both profile editing journeys, Calendar connections at standard/actual largest text, and the local connection action all pass. Native compilation and `git diff --check` pass.
- Checkpoint commit: `4ebf7a7` — Plans recovery and Calendar controls.

## Verified focused profile updates

- The native profile editor currently resends every editable field for any save. The existing PATCH schema explicitly supports partial updates, and the handler updates only supplied values (while maintaining alumni/student year rules). A small native fix can therefore prevent unrelated nickname/contact edits from resubmitting the conflicting semester value without changing server limits.
- Isolated the editor's existing request construction in its draft model so the actual encoded request can be verified. Regression scenarios cover a nickname edit on semester 20, clearing an existing handle, and transitions between alumni/current-student status. This is not a change to registration eligibility or the unresolved server range policy.
- `/tmp/sideseat-ios-uxui-profile-patch-r15.xcresult`: 14 of 16 profile checks passed using the old request behavior. Both new partial-update checks failed as expected; the encoded nickname-only edit included `semester: 20` and eleven other unrelated values. Updated the request construction to omit unchanged values while retaining explicit empty strings for cleared fields and the year/semester associated with a changed student status.
- `/tmp/sideseat-ios-uxui-profile-patch-r16.xcresult`: all 16 profile checks passed, including the three request regressions. Both UI journeys passed: editing and saving several fields through keyboard navigation, and changing school through the existing warning/confirmation/verification/archive flow. Added a deterministic Chinese locale to the latter existing test. No new UI step or server change was introduced.
- Checkpoint commit: `ed5c63f` — save only changed profile fields.

## Verified feedback input guidance

- Existing API rules require message length 10–4,000 and optional title length 3–120. The composer only disabled Send for a short message without explaining why, omitted short supplied titles, and allowed overly long input to reach a server error.
- Added a quiet message-length hint and contextual title guidance in English, German and Chinese. Send now follows the existing contract, retains a valid supplied title, and shows progress while submitting. Title's keyboard Next moves directly to Message; scrolling dismisses the keyboard. The form and dismissal are disabled only while the request is in flight, matching other native forms.
- Extended the existing local-only feedback journey to verify the short-message/short-title explanations, keyboard transition, successful submission, vote toggling, comment creation and return to the new post. It uses the existing DEBUG fixture for all mutations and does not submit real feedback.
- `/tmp/sideseat-ios-uxui-feedback-r17.xcresult`: the complete extended journey passed. Screenshot review confirms both input hints remain visible above the keyboard, and the short title is explained while Send stays disabled. The corrected title is retained after posting and returning from the existing thread. Native compilation, all three strings-file syntax checks and `git diff --check` passed.
- Checkpoint commit: `d82ccba` — feedback input guidance.

## Verified Plan keyboard-space simplification

- Added a real largest-German text journey that opens Plan directly from an existing chat and enters title/location with the keyboard. It checks that the title stays below navigation and above the pinned submit action; all sends use the existing local fixture.
- `/tmp/sideseat-ios-uxui-plan-large-r18.xcresult` did not reach the composer: the test looked for the Leo row while the cached-chat fixture uses `ui-connection`. Corrected the fixture-specific row identifier; this result is not evidence about Plan layout.
- Also testing one bounded simplification of the deferred Calendar keyboard issue: remove its redundant SwiftUI DragGesture and let the existing native scroll-dismiss behavior handle dragging. No UIKit gesture infrastructure is added. Restore the gesture if this does not resolve the observed failure.
- `/tmp/sideseat-ios-uxui-plan-calendar-r19.xcresult`: Plan title/location entry and local submission passed at real largest German text. Calendar scroll dismissal still failed, so restored the original gesture exactly; there is no Calendar editor production diff.
- Plan screenshots showed the pinned instructional paragraph consuming roughly a quarter of the screen, plus decorative field/header icons growing outside their fixed columns. Removed the repeated dock paragraph: the existing in-content notice already explains acceptance and calendar effects. Capped only decorative field/header icons; editable text still follows the full preferred size. This reduces visual and scrolling overhead without changing timing, consent or submission behavior.
- `/tmp/sideseat-ios-uxui-plan-r20.xcresult`: both the real largest-German keyboard journey and the complete light/dark counterproposal/outcome journey passed. Screenshots confirm the pinned area now contains only the primary action, normal/dark forms retain the acceptance notice, and the enlarged text no longer competes with oversized field/header icons. Native compilation and `git diff --check` passed.

- Checkpoint commit: `a570d29` — Plan keyboard-space simplification.

## Verified first-use tutorial simplification

- `/tmp/sideseat-ios-uxui-tutorial-r21.xcresult`: the largest-German journey fails because Next is outside the screen. Screenshot evidence also shows huge decorative icons overlapping the title, duplicate miniature navigation/progress, and both Skip and Close. The normal-text test fails because its accepted German body copy is stale and language was not fixed; it is not evidence of missing product copy.
- Replaced the duplicated controls with one progress counter, one 44pt Skip action, Back and the shared product Next/Done action. A bounded explanation switches to scrolling when it does not fit, keeping navigation outside the scroll view. Decorative icons are omitted at accessibility sizes; the regular icon is neutral and the repeating pulse is removed. Added the missing German Skip translation.
- Normal-text coverage now uses an explicit English language and preferred size. Extended largest-text coverage to scroll to the hint, go forward/back and finish all five steps; the normal journey verifies Skip. Added the same DEBUG authenticated-fixture guard used by other mutations before exercising dismissal, so these tests never write tutorial state to the service.
- `/tmp/sideseat-ios-uxui-tutorial-r22.xcresult`: the complete largest-text journey passed. Screenshot review then found the inherited ignored safe area let the header overlap the status bar. Removed that override; the explanation remains bounded. The normal test still referenced obsolete body copy in every language, so aligned it with the existing translations without changing the product description.
- `/tmp/sideseat-ios-uxui-tutorial-r23.xcresult`: screenshots confirm the safe-area correction and compact normal layout. Two test-only issues prevented completion: iOS does not expose a StatusBar descendant under this app, and a mathematically 44pt button is reported as 43.99999999999994. Use the observed navigation-bar boundary and subpixel tolerance. The sole exit now says Close (existing localized text), avoiding German Skip breaking inside a long word at maximum text size.
- Added targeted local tests for two controller behaviors found during inspection: reading tutorial copy after changing language, and returning to the app while dismissal synchronization is still pending. These exercise the real controller and an in-memory HTTP transport; no live account is involved.
- `/tmp/sideseat-ios-uxui-tutorial-r24.xcresult`: both complete UI journeys pass, and screenshots confirm full German Close text, reachable navigation, readable scrolling and a compact standard card. The new language test reproduces stale English title/body after switching to German; the five existing localization tests pass. The method-level Swift Testing selector omitted the synchronization case, so it was rerun by suite.
- `/tmp/sideseat-ios-uxui-tutorial-sync-r25.xcresult`: the synchronization regression reproduces both the still-present tutorial and old selected tab while its HTTP response is deliberately pending. Ten existing authenticated-network checks pass; one existing validation assertion compares system language against the app's chosen language. Aligned that assertion with the production localization mechanism.
- The controller now resolves tutorial copy in the current language and closes immediately after storing local dismissal. Synchronization still completes normally, updates only the matching account, and does not close a subsequently replayed tutorial. Removed the now-unnecessary dismiss/loading state and miniature-navigation jump helper. The sole exit reuses the existing Close translations; no new strings are needed.
- `/tmp/sideseat-ios-uxui-tutorial-r26.xcresult`: all language, authenticated-network and navigation checks pass, including both new controller regressions. Both UI journeys pass again after the controller changes. Native compilation and `git diff --check` pass. The tutorial now has one progress indicator, one exit and one primary next action; standard and actual maximum German text are visually verified.

- Checkpoint commit: `72ae4f4` — simpler first-use tutorial, immediate dismissal and current-language replay. Acceptance included 37 logic checks and both UI journeys.

## Verified course management improvements

- The manual-create API (`app/api/v1/courses/manual/route.ts`) always creates for the authenticated user's profile school. The sheet currently shows the selected catalog school, which can be different. Added a journey that browses LMU while the fixture user belongs to TUM, then opens manual creation and checks the school before typing. All creates and detail loads use existing DEBUG fixtures.
- The same form has two fields but no keyboard Next/Done behavior or visible explanation of its two-character minimum. The existing API contract is name 2–160 characters, optional code at most 40. Preserve those rules and surface them before submission.
- The existing largest-text course test was only overriding the root environment. It now also sets UIKit's real largest preferred size, explicitly uses German and captures the controls before scrolling, so layout decisions will use actual screenshots.
- `/tmp/sideseat-ios-uxui-courses-r27.xcresult`: the manual flow reproduces “School, LMU” for the TUM user. Its labeled-content accessibility element combines label and value, so the corrected expectation is “School, TUM”. Largest-text course rows pass existing reachability checks, but the screenshot shows unscaled tiny segmented choices and a decorative school icon enlarged beyond its column.
- Manual creation now displays the profile school, explains the school/community destination and existing input limits, focuses Name at standard sizes, moves Next to optional Code and uses Done to dismiss the keyboard. Inputs and dismissal pause only during saving. No create API or enrollment behavior changed.
- At accessibility sizes, the three course scopes use a full-size menu instead of the tiny segmented control. School/review/archive decorative icons are omitted at those sizes and capped otherwise; course content continues scaling fully. Extended the existing journey to switch Popular → My courses through the menu before inspecting course rows.
- `/tmp/sideseat-ios-uxui-courses-r28.xcresult`: all three journeys pass: real largest-German scope switching and course rows, normal course detail/enrollment/remove-cancel, and browsing LMU → creating for TUM with automatic Name focus, visible minimum guidance, keyboard Next to Code and local creation/detail navigation. Screenshot review confirms readable scope text, no oversized school icon, correct school and both input hints above the keyboard. Native compilation, all three strings-file syntax checks and `git diff --check` pass.

- Checkpoint commit: `ec16191` — course school/keyboard guidance and readable scope controls.

## Verified language and privacy settings simplification

- Availability sharing's old chat attachment tray is explicitly retained for compatibility but no longer rendered by the MVP composer; the old test targets an unavailable attachment button. There is no current chat entry to exercise. Kept the streamlined Plan/text composer and legacy card support unchanged instead of restoring an additional flow for QA.
- Continuing with active language and privacy sheets. Language Done currently writes even when no selection changed, an empty selection disables Done without explanation, and these sheets allow edits/dismissal while an existing save is in flight. Inspect focused fixes without adding steps or changing privacy defaults.
- Added a five-second delay only inside the existing DEBUG profile/language fixture mutation when requested by a UI-test launch argument. `/tmp/sideseat-ios-uxui-language-privacy-r29.xcresult` reproduces the editable privacy switch during saving. The language test incorrectly treated the disappearing Done button (replaced by a spinner) as dismissal, then hit the still-open sheet; tightened the assertion to wait for the sheet itself to disappear.
- Language Done now dismisses locally when selection is unchanged, keeps existing proficiency values for actual saves, and explains the minimum selection above the list. It uses the concise Languages title and existing confirmation style. Both sheets disable editing and dismissal only during saving. The shared primary button retains its original accessibility label when displaying progress.
- `/tmp/sideseat-ios-uxui-language-privacy-r30.xcresult`: all six localization checks and all three UI journeys pass. Unchanged language completion skips the artificial delay; changed selections and privacy settings remain disabled during the pending save and persist when reopened. The large-text screenshots expose excessive chrome: Privacy's custom title and bottom save area consume most of the screen, while the new language explanation is unnecessarily long.
- Simplified Privacy to a native settings Form with a concise title and one toolbar Save action, preserving all three switches, explanations, defaults and the explicit save boundary. Removed its decorative section icons and oversized custom header/footer. Shortened language guidance to “Choose at least one language” and made its typography a footnote while retaining full Dynamic Type scaling.
- `/tmp/sideseat-ios-uxui-language-privacy-r31.xcresult`: language behavior passes. Native Form exposes the switch's whole label as its accessibility frame, so tapping its midpoint hit text without changing the value. Updated the UI journey to target the native switch thumb and assert the value immediately. The maximum-text screenshot also shows the switch competing with the entire explanatory paragraph; moved explanations below the switch/title so they use the full row width, and shortened the German suggestion label without changing its meaning.
- `/tmp/sideseat-ios-uxui-language-privacy-r32.xcresult`: the full privacy save/reopen journey passes, including disabled edits/cancel and a stable Save accessibility label during the five-second local delay. The largest-text test still rejects the native toolbar's 36pt reported visible frame; adding a 44pt SwiftUI label frame does not change that native measurement. Removed that ineffective customization and replaced the inherited custom-button geometry assertion with actual maximum-text switch and Save interaction coverage. This result does not establish a 44pt native toolbar hit region.
- `/tmp/sideseat-ios-uxui-language-privacy-r33.xcresult`: both privacy journeys pass, including actual maximum-German switch and Save interaction. Screenshot review found the long title still competing with the switch, so at accessibility sizes the native switch now sits below its full-width title. Its native accessibility label remains present; the duplicate visual title is hidden from VoiceOver.
- `/tmp/sideseat-ios-uxui-language-privacy-r34.xcresult`: the maximum-German journey passes again. Screenshot review confirms the full title, reachable switch and persistent native Save/Cancel controls. The normal layout and save lifecycle were verified in r33, unchanged language completion and actual language edits in r31, and all six language checks in r30. Native compilation, three localization syntax checks and `git diff --check` pass.

## Deferred / blocked

- Signup allows semester 1–20 (`SignupSheet` and `lib/validators/auth.ts`), while profile updates use a maximum of 14 (`profileObjectSchema` / `MAX_SEMESTER`) and the native profile control also caps at 14. The native partial-update fix above prevents unchanged semesters from blocking unrelated edits. The actual semester-range disagreement still requires a coordinated product/API resolution; registration eligibility and concurrent web work were not modified.
- Calendar scroll-dismiss remains reproducible in the inherited editor: `/tmp/sideseat-ios-uxui-auth-and-flows-r3.xcresult`, `calendar-settings-r4.xcresult`, and `auth-settings-r6.xcresult` all keep the keyboard visible. Retargeting from the keyboard/Notes input to the form gutter did not resolve it. Background tap and Done already provide dismissal. No speculative UIKit gesture layer was added; restored the inherited scroll gesture test and deferred this issue to continue other UX improvements.
- Removing the redundant SwiftUI drag recognizer alone also failed in `plan-calendar-r19.xcresult`; the exact original editor code was restored. Further work needs a fresh explanation of the Form/sheet interaction, not more arbitrary gesture variations.
- Auth r5: five localization unit checks and password reset passed. Normal signup screenshot confirms full German toolbar title, concise degrees and translated semester. Semester is exposed as a Stepper, not a separate static text, so its locator was corrected.
- Auth/settings r6: the real largest signup guidance passed its text-height assertion, but a subsequent oversized test drag began on the navigation bar and dismissed the sheet. Login reset targeting oscillated because a partially keyboard-covered button was judged by its midpoint. Narrowed gestures to the visible content, use full target bounds and cap movement to the required distance. Settings screenshot is readable with stacked values; removed the invalid intrinsic-width assertion. Current Settings implementation shares a small adaptive row label between language and notifications.


- The Codex project's configured directory `/Users/baichu/Desktop/app开发项目/Sideseat` contains only stale Xcode workspace metadata. Actual source was located automatically in the path above; no source recovery was required.
- README's four-tab description was stale; current PRODUCT and USER_FLOW explicitly approve five destinations. Updated README to match, preserving native navigation.
- First Xcode attempt stalled in a compiler-probe `write` call (sample saved at `/tmp/sideseat-clang-sample.txt`). Cancelled only this run and restarted with a PTY and `-quiet`; compilation resumed normally. Second baseline result: `/tmp/sideseat-ios-uxui-baseline-r2.xcresult`.
- Stable verification recipe: `build-for-testing -quiet` in a PTY, then `test-without-building -xctestrun /tmp/sideseat-ios-uxui-derived/Build/Products/SideSeat-Development_iphonesimulator26.5-arm64.xctestrun`, with the dedicated simulator, parallel testing disabled, and `-collect-test-diagnostics never`. Automatic simulator diagnostics after assertion failures otherwise delayed completion for many minutes. Screenshots and test attachments remain available in the result bundle.

## Next inspection

- Inspect slow and offline launch: verify existing cached content is usable while the connection is restored and incomplete setup remains gated.
- Continue inspecting common creation/editing flows for unnecessary decisions or lost input; retain the separately logged Calendar keyboard and semester-contract issues.
- Prefer fewer steps and clearer next actions over adding controls, decoration or generalized frameworks.
