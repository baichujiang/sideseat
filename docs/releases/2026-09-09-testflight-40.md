# TestFlight 1.0.0 (40) — discovery-first preparation

## Authority and status

- Owner authorized commit/push → backend with the new discovery flag OFF →
  internal TestFlight → activation only after both phones update.
- Release in progress. Apple currently lists 39 as the latest uploaded build;
  40 is the new unique build number. No upload or phone acceptance claimed yet.
- Target: SideSeat, `app.sideseat.mobile`, Apple app `6800410369`, team
  `V4238R5R53`, existing `SideSeat Internal` group only.
- No external beta, public submission, new testers or real-user data seeding.
- Clean committed release checkout excludes the main workspace's unrelated
  Info.plist edit, old archives and local motion recording.

## Scope and rollout

[Discovery-first Together](../DISCOVERY_FIRST_MATCHING.md) ranks active candidates
by relevance without soft activity/time/language/school/course exclusions. Cards
show actual activities and differences; privacy, safety, explicit refusals and
bilateral consent remain protected. No fabricated historical feedback.

- Keep `V2_DISCOVERY_MATCHING_ENABLED=0` at project, build and runtime scope.
- Preserve `V2_AUTOMATIC_MATCHING_ENABLED=1` and `V2_FLEXIBLE_TIMING_ENABLED=1`.
- No new database migration. Review existing status; do not apply migrations or
  alter user Intent, Plan, Outcome or Shared Encounter data for this release.
- Pre-release compatible rollback: `dpl_Guxz1ysEPtH9yGr9TZRgtEjz3DqY`, source
  `503e827`, with automatic/flexible timing ON.

## Verification baseline

Current feature evidence: 26 combined local PostgreSQL/pure checks passed,
227 V2 checks passed (136 database-dependent skips, focused DB suite run separately),
170 OpenAPI operations and 13 contract tests. Native: 46 model/store and two UI
tests passed, including Chinese Light, English Dark, German largest text and both
empty-state actions. Final result `/tmp/SideSeatDiscovery-20260909-5.xcresult`.

The local two-account primary path verifies **0/100 → private interest → mutual
chat → accepted Plan → both Calendars**. These are local QA results, not signed
Build 40 acceptance or organic Layer 2 Gate evidence.

## Next step

Finish flags-OFF deployment, archive/signature/dSYM validation and internal
distribution. Then ask the owner to confirm **both phones use 1.0.0 (40)** before
enabling discovery and conducting signed two-account acceptance. Real-device
APNs delivery and actual event symbolication remain physical follow-ups.

## Chinese What to Test

Build 40：发现优先匹配准备版（新规则尚未开启）。

请两台测试手机更新到 1.0.0（40），并确认两台均更新完成。本次仅供内部测试。
现有自动寻找同行和弹性时间继续开启；新的发现优先规则暂时关闭，
待两台更新确认后再开启。更新后暂时仍按现有规则推荐，属于预期行为。

1. 更新后先检查登录、同行、已有消息、计划和日历正常加载。
2. 开启新规则后，活动、时间、语言或学校不同也可被发现，按相关度排序；
   卡片应明确展示双方活动和需要商量的差异，低分不代表不能同行。
3. 两个不同账号主动发布意向 → 双方有兴趣 → 聊细节 → 确认具体时间的计划
   → 双方日历。单方兴趣保持私密，有兴趣不等于确认计划。
4. 暂停、结束、屏蔽和明确拒绝仍然有效；不会自动发布旧意向或替用户回答。
5. 检查中英德文、深色模式、大字体、滑条反馈和未发布/暂无新机会的提示。

请记录问题步骤、截图和发生时间。QA 不计入真实用户 Outcome pilot Gate。
