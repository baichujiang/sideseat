# TestFlight 1.0.0 (40) — discovery-first preparation

## Authority and status

- Owner authorized commit/push → backend with the new discovery flag OFF →
  internal TestFlight → activation only after both phones update.
- Backend deployed with discovery OFF. Build 40 is processed and available in
  `SideSeat Internal`; Chinese testing notes are saved. Both phone updates and
  signed acceptance remain pending.
- Target: SideSeat, `app.sideseat.mobile`, Apple app `6800410369`, team
  `V4238R5R53`, existing `SideSeat Internal` group only.
- No external beta, public submission, new testers or real-user data seeding.
- Clean committed release checkout excludes the main workspace's unrelated
  Info.plist edit, old archives and local motion recording.

## Scope and rollout

[Discovery-first Together](../DISCOVERY_FIRST_MATCHING.md) ranks active candidates
by relevance without soft activity/time/language/school/course exclusions. Cards
show actual activities and differences; privacy, safety, explicit refusals and
bilateral consent remain protected. No fabricated historical feedback.

- Keep `V2_DISCOVERY_MATCHING_ENABLED=0` at project, build and runtime scope.
- Preserve `V2_AUTOMATIC_MATCHING_ENABLED=1` and `V2_FLEXIBLE_TIMING_ENABLED=1`.
- No new database migration. Review existing status; do not apply migrations or
  alter user Intent, Plan, Outcome or Shared Encounter data for this release.
- Pre-release compatible rollback: `dpl_Guxz1ysEPtH9yGr9TZRgtEjz3DqY`, source
  `503e827`, with automatic/flexible timing ON.

## Verification baseline

Current feature evidence: 26 combined local PostgreSQL/pure checks passed,
227 V2 checks passed (136 database-dependent skips, focused DB suite run separately),
170 OpenAPI operations and 13 contract tests. Native: 46 model/store and two UI
tests passed, including Chinese Light, English Dark, German largest text and both
empty-state actions. Final result `/tmp/SideSeatDiscovery-20260909-5.xcresult`.

The local two-account primary path verifies **0/100 → private interest → mutual
chat → accepted Plan → both Calendars**. These are local QA results, not signed
Build 40 acceptance or organic Layer 2 Gate evidence.

## Next step

Release is complete through internal availability. Ask the owner to confirm
**both phones use 1.0.0 (40)** before
enabling discovery and conducting signed two-account acceptance. Real-device
APNs delivery and actual event symbolication remain physical follow-ups.

## Backend release evidence

- Source `f7d59392bfb0380c3618b1dc6ee95828d1d571ba` was committed and pushed to
  `feat/native-ui-design-system`. Isolated checkout: `/private/tmp/sideseat-build40.S2St7X`.
- Clean-source release configuration: **68 passed**, no failures or skips.
  Full lint, TypeScript and OpenAPI validation pass. OpenAPI client and Xcode
  project regeneration leave no tracked changes.
- Read-only migration status confirms **134 migrations, schema up to date** on
  the reviewed production Neon target. No migration command or fixture write ran.
  Existing pre-Build-39 recovery branch remains recorded in that release audit;
  no database restore or new recovery branch was necessary for this schema-free release.
- Added the production discovery variable with value `0`, while retaining existing
  automatic/flexible settings. Build and runtime overrides explicitly set discovery
  `0`, automatic `1`, flexible `1`; build explicitly skips database migrations.
- Vercel production-target candidate: `dpl_2R3QJJVaWR18McE5qYWGpWyN93ZL`,
  `https://sideseat-etphzmn9c-baichus-projects.vercel.app`.
  Next.js 15.5.23, READY `2026-09-09T05:39:16.892Z`, build/deploy 154.1 seconds
  after the initial queue. Source metadata matches `f7d5939`.
- Metadata `gitDirty=1` reflects the local native build-tools symlink;
  `ios-native/` is excluded from Vercel uploads. Tracked source is clean.
- Two existing QA accounts passed candidate smoke at `2026-09-09T05:40:14.085Z`,
  then production promotion and smoke at `2026-09-09T05:40:35.538Z`.
  Login, private Plan projections, inbox and Calendar reads pass. Both temporary
  sessions were revoked. No opportunity generation/list, intention read lifecycle,
  publication, interest, Plan or Outcome mutation was requested.
- Both endpoints confirm discovery OFF; automatic/flexible/activity-fit, Together,
  Meet Again and production APNs flags remain ON.
- Public AASA identity/share-route, Privacy and Support checks passed at
  `2026-09-09T05:40:36.915Z`. This is HTTP evidence, not physical Universal Link
  or notification-delivery acceptance.
- Initial deployment-scoped error query since readiness returned no records.
  No log drain was inspected or changed; this bounded observation is not ongoing
  monitoring or future error-free certification.
- Full public-release readiness is **not passed**: Vercel's local environment
  download omits sensitive values, so that local checker cannot certify server
  credentials. Provider metadata/live smoke were checked separately. Previously
  recorded independent cron monitoring and backup/restore-drill configuration
  gaps remain open; no monitoring URL or restore date was invented.

## Native packaging evidence

- Production archive succeeded; SideSeat app debug information uploaded to Sentry.
  Binary/dSYM UUID: `F3F31080-35AE-3853-9DBB-F569F4AC722A` (arm64).
- Archive validation passed at `2026-09-09T05:41:55.022Z`; independent distribution
  IPA validation passed at `2026-09-09T05:42:29.100Z`. Version/build 1.0.0 (40),
  production API/environment, bundle/team identity, encryption declaration,
  minimum iOS 17/iPhone-only, crash DSN and matching binary/dSYM UUID verified.
- Exported IPA signature and embedded Store profile have production APNs,
  `get-task-allow=false`, no device allowlist, and `applinks:www.sideseat.de`.
  Archive development signing is not confused with the distribution signature.
- Compiled resources include 20 animal avatars, six activity artworks and
  Chinese/English/German relevance, activity/time differences and interest labels.
- Validated export SHA-256:
  `400d6460688ed981a37d53e4b55a334cfadea7d4335764f59135ff9ba08e194a`.
- Apple accepted upload at `2026-09-09T05:43:49.329Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED`, exit 0.
- Apple console showed Build 40 **Complete** at approximately
  `2026-09-09 05:46 UTC`. Build ID: `7fe00b1a-2122-4cc0-9389-1c357074fc09`.
  Details confirm exactly one group: **SideSeat Internal**, Internal, one existing
  tester. Existing automatic distribution assigned the build; no group/tester or
  access expansion was added. No external Beta review or App Store submission.
- Chinese What to Test below was saved; the console displayed disabled **Saved**
  at approximately `2026-09-09 05:47 UTC`. Browser-skill observe/interact/verify
  workflow used the existing CUA session because the browser CLI was unavailable.
  Initial loading placeholders were not mistaken for the final group result.
- No phone update, physical acceptance or actual-event symbolication is claimed.
- Non-blocking vendor-symbol warning: Sentry.framework dSYM UUID
  `C0E389A1-0DFE-3961-BF38-2D067FF2817F` was missing. Exact SideSeat application
  symbols uploaded successfully; full vendor-symbol coverage is not claimed.

## Local release artifacts

- Archive target: `/tmp/sideseat-build40.xcarchive`.
- Distribution export target: `/tmp/sideseat-build40-export/SideSeat.ipa`.
- Payload validation: `/tmp/sideseat-build40-verify-payload.mjs`.
- Logs: `/tmp/sideseat-build40-{release-config,openapi,tsc,lint,migration-status,backend-deploy,deployment-status,candidate-smoke,promote,production-smoke,public-smoke,archive,export,archive-validation,ipa-validation,upload}.log`.
- The current temporary compiler-probe launcher buffers and forwards Apple's
  exact stdout/stderr/status, avoiding the observed Xcode pipe deadlock; no macros
  are filtered, and actual compilation uses Apple's clang unchanged.

## Chinese What to Test

Build 40：发现优先匹配准备版（新规则尚未开启）。

请两台测试手机更新到 1.0.0（40），并确认两台均更新完成。本次仅供内部测试。
现有自动寻找同行和弹性时间继续开启；新的发现优先规则暂时关闭，
待两台更新确认后再开启。更新后暂时仍按现有规则推荐，属于预期行为。

1. 更新后先检查登录、同行、已有消息、计划和日历正常加载。
2. 开启新规则后，活动、时间、语言或学校不同也可被发现，按相关度排序；
   卡片应明确展示双方活动和需要商量的差异，低分不代表不能同行。
3. 两个不同账号主动发布意向 → 双方有兴趣 → 聊细节 → 确认具体时间的计划
   → 双方日历。单方兴趣保持私密，有兴趣不等于确认计划。
4. 暂停、结束、屏蔽和明确拒绝仍然有效；不会自动发布旧意向或替用户回答。
5. 检查中英德文、深色模式、大字体、滑条反馈和未发布/暂无新机会的提示。

请记录问题步骤、截图和发生时间。QA 不计入真实用户 Outcome pilot Gate。
