# TestFlight 1.0.0 (39) — intention-driven matching preparation

## Authorization and status

- Owner authorized commit/push → two pending migrations → backend with
  `V2_FLEXIBLE_TIMING_ENABLED=0` and `V2_AUTOMATIC_MATCHING_ENABLED=0` → internal
  TestFlight on 2026-09-09. Activation and external/public distribution are excluded.
- Feature source: flexible timing `71e5241`, automatic publication `46b0e7a`.
- Uploaded, Apple processing complete and available in `SideSeat Internal`.
  Chinese testing notes are saved. Both new rollout flags remain OFF.
- Target: SideSeat, `app.sideseat.mobile`, Apple app `6800410369`, team
  `V4238R5R53`, existing `SideSeat Internal` group. Latest verified Apple build: 38.
- Package from a clean detached worktree. Preserve the main workspace's unrelated
  Info.plist edit, old archives and local motion recording; do not include them.

## Scope and prior verification

Publish an intention → automatic matching → private bilateral interest → chat →
explicitly timed and accepted Plan → both Calendars. Flexible timing supports
undecided/range/exact choices. Old saved intentions are not automatically enrolled.
Both new features stay OFF for this release; existing activity-fit rules stay ON.

See [automatic matching](../INTENT_DRIVEN_MATCHING.md) and
[flexible timing](../FLEXIBLE_INTENT_TIMING.md) for the full local evidence:
15 real PostgreSQL tests, V2 suite 223 pass / 132 database-dependent skipped,
170 OpenAPI operations and 13 contract tests, native model and three-language
UI verification. This evidence is not signed TestFlight/phone acceptance.

## Next step

Have both phones update to 1.0.0 (39). After owner confirmation, separately
authorize activation and accept
the two-account publish → opportunity → chat → Plan → both Calendars flow.
Do not create or rewrite Outcome data. QA does not count toward the real-user Gate.

## Database and rollout evidence

- Source/package revision: `503e8273a1407fab9007c05821b9c1d4ba2f4a2e`, pushed
  to `feat/native-ui-design-system` with the feature commit.
- Verified Vercel project `sideseat` (`prj_Mmv4C0ROdAmglCHaI41vrzTXiH2N`),
  team `baichus-projects`; production API `https://api.sideseat.de`.
- Before migration, created Neon data-and-schema recovery branch
  `pre-intent-matching-20260909-503e827` (`br-withered-wave-apaa9y0h`) from
  production `main` (`br-frosty-snow-apffdw0p`) in project
  `hidden-star-16421165`. The console confirmed creation and expiry at
  `2026-09-16 02:45 UTC` (minute precision). Configured history retention remains
  six hours; this separate seven-day recovery point is not seven-day PITR.
- `prisma migrate status` showed exactly these two pending migrations:
  `20260909010000_flexible_intent_timing` and
  `20260909020000_intent_driven_matching`. The live main endpoint and `neondb`
  database were checked against Neon before execution; no credentials were logged.
- Preflight noticed a historical checksum difference for
  `20260215180000_schedule_share_mvp`. Its local SQL is byte-for-byte unchanged
  from the currently deployed `bbb6a3c` source. No historical migration, checksum
  or migration record was rewritten or resolved.
- Applied only the two new migrations with one-command remote approval.
  At `2026-09-09T02:49:17.835Z`, status was up to date: **134 applied**, zero
  automatically enrolled intentions. No Outcome, Plan or QA fixture writes.
- Both production variables are explicitly `0`: `V2_FLEXIBLE_TIMING_ENABLED`
  and `V2_AUTOMATIC_MATCHING_ENABLED`. Other production settings were preserved.
  Remote migration opt-in is not persisted. Candidate builds explicitly skip migrations.
- Prior production deployment, retained as compatible rollback target:
  `dpl_DJCXoD7q7tuvNmFpabKdAAZ3zrNm`.
- Production-target candidate `dpl_BRvC7393JieuQp7WagtG9XtYD4Wp`
  (`https://sideseat-n6xma36g7-baichus-projects.vercel.app`) was Ready at
  `2026-09-09T02:52:42.703Z`, after `--prod --skip-domain` deployment.
  Next.js 15.5.23 build passed, with migrations explicitly skipped.
- Deployment metadata confirms source `503e827`. Its `gitDirty=1` reflects
  local build tooling/archive artifacts under `ios-native/`, which is excluded
  from Vercel upload. Tracked release-source diff was empty; no private native
  Local.xcconfig or archive was uploaded to the backend.
- Both existing QA accounts passed protected-candidate API smoke by
  `2026-09-09T02:53:45.210Z`, then the deployment was promoted. Both accounts
  passed again on the production API by `2026-09-09T02:53:54.136Z`: login,
  private Plan DTOs, intentions, inbox and Calendar schedule. Each smoke session
  was revoked. No Opportunity list/match or decision/Plan/Outcome write was
  requested; normal intention-read expiry cleanup is not a new publication.
- Both candidate and production report `v2FlexibleTiming=false` and
  `v2AutomaticMatching=false`; existing activity-fit, Together, Meet Again and
  production APNs flags remain enabled.
- Production public checks passed at `2026-09-09T02:54:34.645Z`: API/www AASA
  contain the expected app identity and share-link routes; Privacy and Support
  return HTTP 200. This is HTTP/configuration evidence, not device Universal
  Link or APNs delivery acceptance.
- Deployment-scoped error logs since readiness returned no matching records
  during the initial post-rollout check. This is a short bounded observation,
  not continuous monitoring or certification of future error-free operation.

## Local artifacts

- Archive: `/private/tmp/sideseat-build39.m96pfu/ios-native/SideSeat-1.0.0-39.xcarchive`
- Validated distribution IPA: `/tmp/sideseat-build39-export/SideSeat.ipa`
- Validation scripts: `/tmp/sideseat-build39-{verify-payload,api-smoke,public-smoke}.mjs`
- Logs: `/tmp/sideseat-build39-{release-config,tsc,openapi,lint,format,migration,backend-deploy,candidate-smoke,promote,production-smoke,public-smoke,archive,export,archive-validation,ipa-validation,upload}.log`
- Runtime observation: `/tmp/sideseat-build39-runtime-errors.jsonl` and
  `/tmp/sideseat-build39-runtime-errors-status.log`.
- Main workspace's unrelated Info.plist edit, old archives and motion recording
  remain untouched and excluded. Temporary release artifacts stay local.

## Release checks and packaging

- Clean-source release configuration: **68 passed**, zero failed/skipped
  (31 iOS, 11 profile, three verification, 23 operations/readiness).
- OpenAPI client and Xcode project regenerated without tracked differences.
- TypeScript, scoped ESLint, OpenAPI validation (170 operations) and scoped
  documentation formatting pass. Prior feature suites are recorded above.
- Production archive succeeded. Sentry uploaded the exact app debug information
  file, UUID `57366662-2F43-3009-992D-00D07F107297` (arm64).
- Archive validation passed at `2026-09-09T02:51:17.707Z`; independent distribution
  IPA validation passed at `2026-09-09T02:51:38.274Z`. Correct build, bundle,
  production API/environment, encryption declaration, minimum iOS 17/iPhone-only,
  code signature, configured crash DSN and matching app/dSYM UUID all verified.
- The separately distribution-signed IPA has production APNs, correct Team/app
  identity, `get-task-allow=false`, no device allowlist and
  `applinks:www.sideseat.de`. The archive's development signature is not confused
  with the actual distribution IPA.
- Compiled resources include 20 animal avatars, six activity artworks and
  Chinese/English/German publication, resume, flexible-timing and interest labels.
- IPA SHA-256: `06e8838db2d824a1aec970dfe155fe86814f646e1e728d4e7910d1c7a06237a6`.
- Apple accepted the upload at `2026-09-09T02:55:55.470Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED`, exit 0. The console showed
  Build 39 **Processing**, uploaded at 04:55 Berlin time.
- Non-blocking vendor warning: missing `Sentry.framework` dSYM UUID
  `204F9BD4-F080-3CA7-9777-F77D31011545`. The exact SideSeat app dSYM was uploaded
  successfully; full vendor-symbol coverage is not claimed.
- Apple processing completion and internal availability were verified in the
  console around `2026-09-09 02:59 UTC`; the uploaded Build 39 row shows **Complete**.
- Build ID: `a3b49969-e938-4f9a-bd9c-fdd052d91be1`. Build details show exactly
  one group: **SideSeat Internal**, Internal, one existing tester. Existing
  automatic distribution assigned this build; no group, tester or access was added.
- Saved the Chinese What to Test text below; Apple displayed disabled **Saved**.
  The browser skill's observe/interact/verify workflow was used through CUA
  because its CLI was unavailable. An initial empty group placeholder was not
  mistaken for the loaded group result.
- No external beta, Beta review or App Store submission. Neither phone update,
  signed two-account acceptance, real APNs delivery nor symbolication of an
  actual Build 39 device event is claimed. These remain follow-up evidence.

## Chinese What to Test

Build 39：活动意向自动匹配与弹性时间准备版（新流程尚未开启）。

请两台测试手机更新到 1.0.0（39），并确认更新完成。本次仅供内部测试。

当前自动匹配和弹性时间两个服务器开关均关闭，仍使用现有手动匹配流程。
更新后暂时看不到新流程是预期行为；不会自动发布旧意向，也不会自动替你回答。

1. 更新后先检查登录、已有同行卡片、消息、计划和日历能否正常加载。
2. 两台更新确认并另行开启新功能后，再测试：发布活动意向即可自动寻找同行，
   不再需要单独点击开始匹配；暂停、恢复、结束应控制该意向是否继续参与。
3. 开启后检查时间可待商议、明天/周末/下周、日期范围和精确时间。
   时间未定不代表双方全天有空；计划必须商量并确认实际时间。
4. 使用两个不同的 SideSeat 账号：发布 → 有兴趣 → 双方有兴趣后聊细节 →
   发起并确认计划 → 双方日历。单方兴趣保持私密，有兴趣不等于确认计划。
5. 检查中英德文、深色模式、大字体及滑条反馈。记录问题步骤、截图和发生时间。

QA 演示对象不会自动回答；QA 验收不计入真实用户 Outcome pilot Gate。
