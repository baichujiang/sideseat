# TestFlight release record — 1.0.0 (36)

**Status:** Preparing the new internal client. Upload, Apple processing and
internal group availability are not yet verified.

**Date:** 2026-09-08 (Europe/Berlin)

## Authority and source

The owner authorized committing/pushing the activity-fit improvement and
publishing a new internal TestFlight version. Related matching may be enabled
only after the owner confirms both test phones have updated.

- Feature source: `2ec160d` on `feat/native-ui-design-system`.
- Version/build: `1.0.0 (36)`; App Store Connect was checked and 35 was latest.
- Package from a clean detached worktree, excluding the main workspace's
  unrelated Info.plist edit and old Build 29/30 archives.
- Scope: existing `SideSeat Internal` group only; no external beta or public
  submission and no production data mutation.
- Backend deployment and `V2_ACTIVITY_FIT_ENABLED` activation are not part of
  this native upload. The new client accepts the existing backend's no-score
  responses. The server change must be deployed before the new score/rules can
  become visible; keep related matching disabled until both phones update.
- No database schema change or migration is required for this feature.

## Feature and existing verification

The new client renders activity-fit points and an expandable explanation in
Chinese, English and German, including both original activities when details
still need agreement. Points describe an activity opportunity, not a person's
quality or a calibrated probability.

The exact feature passed the local [activity-fit verification](../MATCHING_ACTIVITY_FIT.md):
32 compatibility/PostgreSQL checks, native decoding and cancellation regressions,
three-language visual checks and a real local API two-account UI flow for a
60/100 related opportunity through bilateral YES, accepted Plan and both Calendars.
Read-only database verification confirmed the persisted result and zero Outcome
answers. These local QA checks do not establish signed TestFlight acceptance or
organic pilot success.

## Packaging and distribution

Pending current release-configuration checks, generation, Production archive,
Sentry app dSYM upload, distribution-payload validation and Apple upload.
Record actual results here after execution; do not infer them from Build 35.

## Next step

After Apple completes processing, verify availability to `SideSeat Internal` and
save Chinese testing notes. Both phones must update through TestFlight and
confirm `1.0.0 (36)` before related matching is activated. Then complete the
reviewed backend rollout and signed two-account matching → private YES → Plan →
both Calendars acceptance. Do not activate the broader policy while unsupported
old clients still participate; external rollout requires a separate decision.
