# TestFlight release record — 1.0.0 (36)

**Status:** Uploaded, Apple processing complete and available to the existing
`SideSeat Internal` group. The broader matching policy remains inactive;
both physical test phones still need to confirm their update.

**Date:** 2026-09-08 (Europe/Berlin)

## Authority and source

The owner authorized committing/pushing the activity-fit improvement and
publishing a new internal TestFlight version. Related matching may be enabled
only after the owner confirms both test phones have updated.

- Feature source: `2ec160d` on `feat/native-ui-design-system`.
- Archive source: `661866a597142944984fdc50632d08ff5d12cbc2`, pushed.
- Version/build: `1.0.0 (36)`; App Store Connect was checked and 35 was latest.
- Package from a clean detached worktree, excluding the main workspace's
  unrelated Info.plist edit and old Build 29/30 archives.
- Scope: existing `SideSeat Internal` group only; no external beta or public
  submission and no production data mutation.
- Backend deployment and `V2_ACTIVITY_FIT_ENABLED` activation are not part of
  this native upload. The new client accepts the existing backend's no-score
  responses. The server change must be deployed before the new score/rules can
  become visible; keep related matching disabled until both phones update.
- No database schema change or migration is required for this feature.

## Feature and existing verification

The new client renders activity-fit points and an expandable explanation in
Chinese, English and German, including both original activities when details
still need agreement. Points describe an activity opportunity, not a person's
quality or a calibrated probability.

The exact feature passed the local [activity-fit verification](../MATCHING_ACTIVITY_FIT.md):
32 compatibility/PostgreSQL checks, native decoding and cancellation regressions,
three-language visual checks and a real local API two-account UI flow for a
60/100 related opportunity through bilateral YES, accepted Plan and both Calendars.
Read-only database verification confirmed the persisted result and zero Outcome
answers. These local QA checks do not establish signed TestFlight acceptance or
organic pilot success.

## Packaging and distribution

- Release-configuration checks: 66 passed, 0 failed, 0 skipped (31 iOS,
  9 profile, 3 verification and 23 operations/readiness checks).
- ESLint and TypeScript (`--noEmit --incremental false`): passed.
- OpenAPI route validation: 170 implemented operations passed.
- OpenAPI and Xcode project regenerated without tracked differences in the
  detached worktree. Existing pinned dependency/tool caches were reused.
- Production archive succeeded at approximately `2026-09-08T06:08:13Z`.
  Production configuration validation and the mandatory Sentry symbol phase passed;
  Sentry uploaded one missing app debug information file.
- App binary and dSYM UUID both equal
  `9043DC38-AD4E-347C-80A6-50F2E1BF97E7` (arm64).
- Archive payload verification passed at `06:08:26 UTC`: version `1.0.0 (36)`,
  bundle `app.sideseat.mobile`, production API/environment, configured crash DSN,
  minimum iOS 17, iPhone-only, non-exempt encryption false and strict signature.
  The automatically signed archive uses a development profile; the App Store
  export must be separately verified for distribution signing and production APNs.
- App Store export succeeded; its payload was independently verified at
  `06:08:54 UTC`. Signed entitlements and embedded distribution profile both have
  production APNs, Team `V4238R5R53`, correct app identity and
  `get-task-allow=false`; no device allowlist. Associated Domains includes
  `applinks:www.sideseat.de`. The exported app retains the exact app/dSYM UUID.
- Apple accepted the upload at `2026-09-08T06:10:51Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED` with exit 0.
- Apple reported the existing prebuilt `Sentry.framework` missing-dSYM warning,
  framework UUID `832E816B-6F09-3B70-884C-2B96D0710CB4`. It did not block upload;
  the separate SideSeat app dSYM upload above succeeded. No vendor-symbol or
  public-release clearance is claimed.

## Apple processing and internal availability

- App Store Connect independently showed Build 36 processing, then Complete;
  processing and existing-group assignment were confirmed by `06:14 UTC`.
- Build ID: `376a673b-1ed8-4044-8bfd-7e780943cf7b`, SideSeat app `6800410369`.
- The loaded build group table confirmed `SideSeat Internal`, type Internal,
  with one existing tester. Automatic assignment required no new group/tester
  or wider access. Availability does not prove either phone has installed it.
- Chinese testing notes explain that this upload is the compatible client first,
  both phones must confirm Build 36, and server deployment/activation remains
  pending. The new matching scores/rules are not yet live. The build page showed
  the disabled **Saved** state after submission at `06:14 UTC`.
- No external group, Beta review, App Store submission or production backend
  deployment/flag change was performed. No phone was controlled or updated.
- The browser-verification guide was applied through the available CUA connection:
  actual build status and group contents were checked, not inferred from Xcode's
  upload success. Signed physical-phone acceptance remains a separate step.

## Local artifacts

- Archive: `/private/tmp/sideseat-build36.Aj0o9U/ios-native/SideSeat-1.0.0-36.xcarchive`
- Validated IPA: `/tmp/sideseat-build36-export/SideSeat.ipa`
- IPA SHA-256: `762aae7128316bb90909293b626d0e9b919e3216718bec9a389f7c3a1cde4a67`
- Logs: `/tmp/sideseat-build36-{generation,release-config,openapi,lint,tsc,archive,export,upload,archive-validation,ipa-validation}.log`.

## Next step

The owner subsequently confirmed both phones have `1.0.0 (36)`. The reviewed
backend has now been deployed with related matching enabled and both QA accounts
passed production API smoke; see the [follow-up rollout record](./2026-09-08-activity-fit-production.md).
Signed two-account matching → private YES → Plan → both Calendars acceptance
subsequently passed on the installed Build 36; the rollout record documents
the QA time alignment and narrow display follow-ups. Next, collect conversion
evidence with existing internal testers. External rollout still requires a
separate compatible-client decision.
