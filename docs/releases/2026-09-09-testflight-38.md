# TestFlight release record — 1.0.0 (38)

**Status:** Authorized; packaging and internal publication in progress.

**Date:** 2026-09-09 (Europe/Berlin)

## Authority and scope

The owner confirmed committing, pushing and publishing the approved native UI
work to internal TestFlight. App Store Connect was checked: Build 37 was latest.

- Branch: `feat/native-ui-design-system`; version/build `1.0.0 (38)`.
- Package from a clean detached worktree, preserving and excluding the main
  workspace's unrelated Info.plist edit and old Build 29/30 archives.
- Existing `SideSeat Internal` group only; no external beta or App Store submission.
- No matching-rule changes, production database writes, migrations or QA reseeding.
- Native avatars are bundled offline and use the existing profile API. The
  corresponding web assets are committed, but no backend/web deployment is included.

## Changes and existing verification

- Twenty original Little Companions avatars and a native preset chooser:
  [avatar evidence](../SYSTEM_AVATARS.md), including a passing local two-account API test.
- Six activity illustrations, clearer opportunity hierarchy, adaptive ink/chalk
  product buttons and reachable largest-text editor actions.
- Left Ignore / right Interested bar with continuous drag feedback, threshold
  rings, release snap and tiered haptics. Interest remains private; mutual interest
  opens chat, and Plan confirmation remains a separate step.
- [Design verification](../DESIGN_SYSTEM.md): 26 contrast/asset checks and six
  distinct comfort UI checks passed. Final motion verification passed four
  distinct UI checks and two gesture unit tests, including Reduce Motion,
  cancellation, vertical scrolling and failed-save recovery.
- These are simulator/local API results, not physical Build 38 acceptance.

## Next step

Finish clean-source release checks, archive, validate the distribution payload,
upload, then confirm Apple processing, internal group availability and saved
testing notes. After availability, update both test phones and verify the avatar,
activity picker and physical pickup/threshold/release feedback through the
private-interest → chat → confirmed Plan path. QA is not an organic pilot Gate pass.
