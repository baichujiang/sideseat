# TestFlight release record — 1.0.0 (38)

**Status:** Uploaded, Apple processing complete and available to the existing
`SideSeat Internal` group. Chinese testing notes are saved. Phone updates and
physical Build 38 acceptance remain the next step.

**Date:** 2026-09-09 (Europe/Berlin)

## Authority and scope

The owner confirmed committing, pushing and publishing the approved native UI
work to internal TestFlight. App Store Connect was checked: Build 37 was latest.

- Branch: `feat/native-ui-design-system`; version/build `1.0.0 (38)`.
- QA tooling: `5fb453a`; native feature: `94ee9ba`; archive source:
  `2aefb88dc2c57d75f9ba88248ee22e8a4b5a5254`. All three commits were pushed.
- Package from a clean detached worktree, preserving and excluding the main
  workspace's unrelated Info.plist edit and old Build 29/30 archives.
- Existing `SideSeat Internal` group only; no external beta or App Store submission.
- No matching-rule changes, production database writes, migrations or QA reseeding.
- Native avatars are bundled offline and use the existing profile API. The
  corresponding web assets are committed, but no backend/web deployment is included.

## Changes and existing verification

- Twenty original Little Companions avatars and a native preset chooser:
  [avatar evidence](../SYSTEM_AVATARS.md), including a passing local two-account API test.
- Six activity illustrations, clearer opportunity hierarchy, adaptive ink/chalk
  product buttons and reachable largest-text editor actions.
- Left Ignore / right Interested bar with continuous drag feedback, threshold
  rings, release snap and tiered haptics. Interest remains private; mutual interest
  opens chat, and Plan confirmation remains a separate step.
- [Design verification](../DESIGN_SYSTEM.md): 26 contrast/asset checks and six
  distinct comfort UI checks passed. Final motion verification passed four
  distinct UI checks and two gesture unit tests, including Reduce Motion,
  cancellation, vertical scrolling and failed-save recovery.
- These are simulator/local API results, not physical Build 38 acceptance.

## Release checks

- Clean-source release configuration: **68 passed**, zero failed/skipped (31 iOS,
  11 profile, three verification and 23 operations/readiness checks).
- Scoped QA catalog: two tests passed; the seeder was not executed during release.
- ESLint, TypeScript (`--noEmit --incremental false`), scoped Prettier and all
  three native localization files passed.
- OpenAPI validation: 170 implemented operations passed. OpenAPI client and Xcode
  project regeneration produced no tracked differences in the release worktree.
- The initial check in the dirty main workspace correctly failed the encryption
  declaration check because of the owner's unrelated Info.plist edit. That edit
  was preserved and excluded; the clean committed release source passed above.

## Packaging evidence

- Production archive succeeded; Sentry uploaded one missing app debug information
  file. App binary/dSYM UUID: `D37AD130-745C-303B-8D35-3596BAA0E857` (arm64).
- Archive validation passed at `2026-09-09T00:05:17.513Z`; distribution export and
  independent payload validation passed at `2026-09-09T00:06:02.519Z`.
- Both payloads have the correct build, bundle ID, production API/environment,
  crash DSN configuration, encryption declaration, iPhone-only/minimum iOS 17,
  strict code signature and matching binary/dSYM UUID.
- Compiled asset catalogs include all 20 `SystemAvatar` and six `ActivityArtwork`
  assets. The actual Chinese/English/German interest-bar strings were checked.
- Distribution entitlements and profile have production APNs, correct Team/app
  identity, `get-task-allow=false`, no device allowlist and
  `applinks:www.sideseat.de`. The development-signed archive is not confused with
  the separately distribution-signed IPA.
- Non-blocking compile warnings remain in pre-existing disabled Discover creation
  paths and AppIntents metadata extraction; no compile errors.
- Apple accepted the upload at `2026-09-09T00:07:36.546Z`: Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED`, exit 0.
- Apple again reported a non-blocking missing vendor `Sentry.framework` dSYM,
  this build's UUID `BCE370E4-DD8F-3687-A7FB-3838758FE8F7`. The exact SideSeat app
  dSYM was uploaded successfully. Vendor-symbol completeness is not claimed.

## Apple processing and internal availability

- App Store Connect showed Build 38 processing, then **Complete**. Internal
  availability and saved notes were verified around `2026-09-09 00:13 UTC`.
- Build ID: `5fc3ce70-6f3d-46f0-8d86-1a68c94e25ad`; app `6800410369`.
- Build details show exactly one group: **SideSeat Internal**, type Internal,
  one existing tester. The group's existing automatic Xcode build distribution
  assigned Build 38 without adding any group or tester.
- Chinese What to Test text below was saved; Apple displayed disabled **Saved**.
- The browser skill's observe/interact/verify workflow was used through the
  available CUA connection because the `agent-browser` CLI was unavailable.
  Processing, the loaded group table and saved notes were verified in the UI;
  availability was not inferred from upload success or a transient empty group list.
- The build list's “Ready to Submit” label concerns external review, not a missing
  internal assignment. No external group, Beta review or App Store submission was
  made. No phone installation, production data write or backend/web deployment
  was performed. Availability is not proof that either phone has updated.

## Local artifacts

- Archive: `/private/tmp/sideseat-build38.ZBxyaM/ios-native/SideSeat-1.0.0-38.xcarchive`
- Independently validated IPA: `/tmp/sideseat-build38-export/SideSeat.ipa`
- IPA SHA-256: `2dd2077ade2a637fac43ffafda922bf2339a07ea5400365114cce60a4c65273f`
- Payload validator: `/tmp/sideseat-build38-verify-payload.mjs`.
- Logs: `/tmp/sideseat-build38-{generation,release-config-clean,openapi,lint,tsc,archive,export,upload,archive-validation,ipa-validation}.log`.
- Large simulator motion recording remains local and untracked; old archives and
  the unrelated main-workspace Info.plist edit were not staged or overwritten.

## Internal testing notes

Chinese What to Test text saved to Apple:

```text
Build 38：全新小动物头像、同行活动插画与卡片、强反馈意向滑条。

请两台测试手机更新到 1.0.0（38）。线上放宽匹配规则保持不变。本次仅供内部测试。

1. 在“我”中更换小动物头像并保存，确认消息及对方个人资料显示一致；取消选择不应修改头像。
2. 在“同行”选择活动与未来共同有空时间，检查六类活动插画、匹配卡片的信息层级及匹配度展开说明。分数只描述活动契合程度，不评价人或承诺成功率。
3. 从滑条中央向右拖动表示“有兴趣”，向左表示“忽略”。检查拿起、拖动、到达阈值、松手时的视觉与震动反馈。短距离松手应回中，竖向拖动应滚动而不回答；保存时不可重复提交。也可直接点击两侧文字。
4. 两个不同的 SideSeat 账号分别表达兴趣后，进入“聊聊细节”，商量后再发起并确认计划；有兴趣不等于确认计划。确认后检查双方日历。
5. 检查深色模式、大字体及系统“减弱动态效果”；特别反馈震动是否过强或过弱。

已有 QA 演示对象不会自动回答；验证双边闭环请使用两台手机的真实操作。请记录问题步骤、截图和发生时间。QA 结果不计入真实用户 pilot Gate。
```

## Next step

Update both test phones to **1.0.0 (38)** and verify the avatar,
activity picker and physical pickup/threshold/release feedback through the
private-interest → chat → confirmed Plan path. QA is not an organic pilot Gate pass.
