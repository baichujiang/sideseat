# TestFlight release record — 1.0.0 (31)

**Status:** Processed and automatically distributed internally; focused physical smoke pending

**Date:** 2026-09-07

**Scope:** Localization completion and the shortest Layer 2 user-flow pilot loop

**Governing procedure:** [Release](../RELEASE.md)

## Recorded evidence

- Source commit: `4ebcd3c` (`chore(release): prepare TestFlight build 31`)
- Localization commit: `a236e38` (`fix(ios): complete in-app localization coverage`)
- Branch: `feat/native-ui-design-system`
- Both commits were pushed to `origin/feat/native-ui-design-system` before the
  archive was created.
- The archive was built from a clean detached worktree at the exact source
  commit. The main workspace's local plist edit and archived build artifacts
  were not included.
- No database migration or backend deployment was required for this native-only
  localization release.
- Archive: `SideSeat-Production`, marketing version `1.0.0`, build `31`.
- Bundle ID: `app.sideseat.mobile`; architecture: `arm64`; minimum iOS: `17.0`.
- Embedded API: `https://api.sideseat.de`; environment: `production`.
- Export compliance: `ITSAppUsesNonExemptEncryption=false`.
- SideSeat app binary and dSYM share UUID
  `D545370E-2AA7-3685-92B8-7FA0B8A06F5B`.
- The guarded Production archive completed its SideSeat dSYM upload phase.
- App Store Connect accepted the upload at `2026-09-07T21:06:17Z` and reported
  that the uploaded package was processing.
- The `SideSeat Internal` group contains one consenting internal tester and has
  automatic distribution of Xcode builds enabled.
- Apple processing completed by `2026-09-07T21:13:37Z`. App Store Connect then
  showed build 31 assigned to the `SideSeat Internal` group with one tester.

The exporter warned that the prebuilt `Sentry.framework` vendor dSYM was not
inside the archive. This does not replace or invalidate the matching SideSeat
app dSYM recorded above.

## Tonight's minimum acceptance

This build intentionally prioritizes the shortest complete pilot flow over a
new exhaustive UI matrix:

1. install or update to TestFlight build 31;
2. log in with a normal account;
3. confirm the selected Chinese interface shows the Plans surface in Chinese;
4. open a completed accepted Plan from Together, Messages or Calendar;
5. submit one private Outcome answer and confirm the saved state is shown.

Build 30 already passed the full two-account Layer 2 Outcome and Shared
Encounter acceptance. Build 31 changes native localization and must still pass
the focused physical smoke above before it is treated as the pilot build.

## Outstanding gates and next step

- Run the five-step focused physical smoke on build 31 and record the result.
- Then recruit consenting non-QA tester pairs and continue collecting truthful
  bilateral Outcome responses.
- The Layer 2 product Gate remains unpassed. Layer 3 must not start before two
  consecutive qualifying snapshots, with the next formal snapshot no earlier
  than `2026-09-14T10:37:35Z`.

Internal TestFlight distribution is not App Store release approval and does not
close the Layer 2 product Gate.
