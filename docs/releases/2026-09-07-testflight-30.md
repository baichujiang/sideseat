# TestFlight release record — 1.0.0 (30)

**Status:** Uploaded; App Store Connect processing

**Date:** 2026-09-07

**Scope:** Layer 2 tester distribution for Outcome and Shared Encounter

**Governing procedure:** [Release](../RELEASE.md)

## Recorded evidence

- Source commit: `92c6818` (`chore(release): prepare TestFlight build 30`)
- Branch: `feat/native-ui-design-system`
- The archive was built from a clean detached worktree at the exact source
  commit. The main workspace's local plist edit and archived build artifacts
  were not included.
- Layer 2 database migrations:
  - `20260902120000_weekly_intent_general_activity`
  - `20260907120000_shared_encounter`
- Layer 2 Vercel production deployment:
  `dpl_HWM19p9xx9pHQQgc9CAAidoa4nkH`
- Production URL: `https://api.sideseat.de`
- Release checks passed:
  - lint and TypeScript type-check;
  - release configuration tests;
  - OpenAPI contract check with 169 implemented operations;
  - production web build;
  - 310 native unit tests across 57 suites.
- Archive: `SideSeat-Production`, marketing version `1.0.0`, build `30`.
- Bundle ID: `app.sideseat.mobile`; architecture: `arm64`; minimum iOS: `17.0`.
- Embedded API: `https://api.sideseat.de`; environment: `production`.
- Export compliance: `ITSAppUsesNonExemptEncryption=false`.
- App Store export used an Apple Distribution certificate and Store
  provisioning profile. Exported entitlements include Production APNs,
  `beta-reports-active=true`, Associated Domains, and `get-task-allow=false`.
- SideSeat app dSYM UUID:
  `D4B06C27-6A45-39B2-9906-24A2DB8D29A7`.
- Sentry accepted the SideSeat app dSYM during the Production archive.
- App Store Connect accepted the upload at `2026-09-07T11:23:17Z` and reported
  that the uploaded package was processing.

The exporter warned that the prebuilt `Sentry.framework` vendor dSYM was not
inside the archive. This does not replace or invalidate the successfully
uploaded SideSeat app dSYM recorded above.

## Tester scope

Test the private Outcome answer from Together, Messages and Calendar history:

- each participant can answer `OCCURRED`, `DID_NOT_OCCUR` or skip privately;
- one participant's answer does not reveal the peer answer or response count;
- a Shared Encounter is created only after both participants answer
  `OCCURRED`;
- no Shared Encounter is created for a unilateral answer, skip or any
  `DID_NOT_OCCUR` answer.

## Outstanding gates and next step

- Wait for App Store Connect processing, then make build 30 available to the
  intended internal tester group.
- Run and record a physical TestFlight two-account smoke test, including
  Production APNs foreground, background and cold-launch delivery.
- Complete the Vercel Production configuration gate: verification Blob token,
  Cron secret and independent monitor URLs, Neon backup/restore metadata, and
  Production APNs server credentials with `APNS_USE_SANDBOX=0`.
- Confirm one non-PII build 30 event is symbolicated in Sentry.
- Continue collecting non-QA bilateral Outcome responses. The Layer 2 product
  Gate remains open; Layer 3 must not start before two consecutive qualifying
  snapshots, with the next snapshot no earlier than
  `2026-09-14T10:37:35Z`.

Uploading this build makes it available for tester validation after Apple
processing. It is not App Store release approval and does not close the Layer 2
product Gate.
