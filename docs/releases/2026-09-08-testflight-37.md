# TestFlight release record — 1.0.0 (37)

**Status:** Uploaded, Apple processing complete and available to the existing
`SideSeat Internal` group. Both phone updates and signed Build 37 acceptance
remain the next step.

**Date:** 2026-09-08 (Europe/Berlin)

## Authority and source

The owner authorized committing/pushing the native title/disclosure fix and
publishing the next internal TestFlight build.

- Feature source: `a5387f7` on `feat/native-ui-design-system`, pushed.
- Archive source: `91c6b4fd7217ff96f4a0eb65e674c49f33f7a693`, pushed.
- Version/build: `1.0.0 (37)`; App Store Connect was checked and 36 was latest.
- Package from a clean detached worktree, excluding the main workspace's
  unrelated Info.plist edit and old Build 29/30 archives.
- Scope: existing `SideSeat Internal` group only. No external beta or App Store
  submission, new testers, database migrations or production data mutation.
- The already-live activity-fit backend and broader matching policy remain
  unchanged. This is a native display release only.

## Feature and verification

See the [native follow-up record](./2026-09-08-native-title-disclosure-fix.md)
for four passing native checks, four language/font visual scenarios and the
real local API two-account UI closure through a Chinese Plan and both Calendars.
The exact feature was verified immediately before this release; the build-number
change does not alter that feature. These are local QA results, not signed
Build 37 acceptance or an organic pilot Gate pass.

- System-generated Together titles use the app language in context displays and
  new Plan prefill. User-authored text and previously saved Plan titles stay intact.
- Expanded activity-fit explanations reserve their full height, including at
  accessibility text sizes; collapse and Reduce Motion remain supported.
- All three localization files and scoped whitespace checks passed before commit.

## Packaging and distribution

- Release-configuration checks: 66 passed, 0 failed, 0 skipped (31 iOS,
  9 profile, 3 verification and 23 operations/readiness checks).
- ESLint and TypeScript (`--noEmit --incremental false`): passed.
- OpenAPI route validation: 170 implemented operations passed.
- OpenAPI and Xcode project regenerated without tracked differences in the
  detached worktree. Existing pinned dependency/tool caches were reused.
- Production archive succeeded by `12:19:35 UTC`. The mandatory Sentry phase
  uploaded one missing SideSeat app debug information file.
- App binary/dSYM UUID: `468F7C55-D574-31D0-B917-8DFDBDE5402F` (arm64).
- Archive validation passed at `2026-09-08T12:19:35.779Z`: correct build/bundle,
  production API/environment, configured crash DSN, non-exempt encryption false,
  iPhone-only, minimum iOS 17, strict code signature and matching app/dSYM UUID.
- App Store export succeeded and independent distribution validation passed at
  `2026-09-08T12:20:08.179Z`. Both signed entitlements and distribution profile
  use production APNs, correct Team/app identity and `get-task-allow=false`;
  no device allowlist. Associated Domains includes `applinks:www.sideseat.de`.
- Both payloads contain the new Chinese/English/German title resources and the
  exact app/dSYM UUID. The automatically development-signed archive is not
  confused with the separately distribution-signed exported app.
- Apple accepted the upload at `2026-09-08T12:22:07.972Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED`, exit 0.
- Apple reported a non-blocking missing vendor dSYM warning for `Sentry.framework`,
  UUID `20412310-66D8-34F3-98CE-CCF5D44D7F09`. The exact SideSeat app dSYM was
  successfully uploaded above; no vendor-symbol or public-release clearance
  is claimed.

## Apple processing and internal availability

- App Store Connect showed Build 37 processing, then Complete; existing-group
  availability and saved notes were confirmed at `2026-09-08 12:26 UTC`.
- Build ID: `8b20c509-34aa-4098-beb1-73d76d5b3dc3`, SideSeat app `6800410369`.
- The loaded group table confirmed `SideSeat Internal`, type Internal, with
  one existing tester. Automatic assignment required no new group or tester.
- Chinese testing notes below were saved; Apple showed the disabled **Saved**
  state. They accurately say broader matching is already active and unchanged,
  unlike the earlier client-first Build 36 notes.
- The browser-verification guide was applied through the available CUA connection:
  actual Apple processing, the loaded group table and saved notes were checked,
  not inferred from Xcode upload success.
- No external group/Beta review/App Store submission, backend deployment,
  feature-flag change, database migration or phone installation was performed.
  Availability does not prove either phone has updated or accepted Build 37.

## Local artifacts

- Archive: `/private/tmp/sideseat-build37.Tk6idx/ios-native/SideSeat-1.0.0-37.xcarchive`
- Validated IPA: `/tmp/sideseat-build37-export/SideSeat.ipa`
- IPA SHA-256: `674676fca7c346fd2e7ed54df9940b295b6f2ee4981f4588d577bd9d7769fa9d`
- Logs: `/tmp/sideseat-build37-{generation,release-config,openapi,lint,tsc,archive,export,upload,archive-validation,ipa-validation}.log`.

## Internal testing notes

Chinese What to Test text saved to Apple:

```text
Build 37：自动计划标题中文化、匹配度展开说明修复。

请两台测试手机更新到 1.0.0（37）。线上放宽匹配规则已开启，本次不修改匹配规则。

1. 中文界面下用两个不同的 SideSeat 账号，选择同一类别的活动、至少 30 分钟重叠的未来时间，双方分别开始匹配。活动描述可以不同，例如“喝咖啡”和“咖啡聊聊”。
2. 在匹配卡片点击“匹配度怎么算”：完整显示计算依据、共同有空时间和分数说明；再点击可收起。放大系统字体后也不应裁切或覆盖下面内容。分数描述活动契合程度，不评价人，也不代表成功概率。
3. 双方私密选择“愿意同行”，进入消息并制定新计划：系统生成的标题应为“一起喝咖啡”等中文。发送并接受后，两边日历应显示同一计划标题。
4. 用户自己填写的活动文字、已创建计划的原有标题不会被自动翻译。英文、德文界面也可检查展开/收起和标题显示。

请记录问题步骤、截图和发生时间。本次仅供内部测试；QA 结果不计入真实用户 pilot Gate。
```

## Next step

Ask both phones to update to Build 37 and confirm the new Chinese Plan prefill
and full expanded explanation through a new accepted Plan and both Calendars. Historical
saved titles are not retroactively translated. Continue real internal matching
to Plan feedback; public distribution remains a separate readiness decision.
