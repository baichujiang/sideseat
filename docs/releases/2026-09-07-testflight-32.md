# TestFlight release record — 1.0.0 (32)

**Status:** Processed and assigned to internal testing; focused physical smoke
pending

**Date:** 2026-09-07

**Scope:** Make pending Plan decisions and private Outcome answers visible in
the shortest Layer 2 pilot flow

**Governing procedure:** [Release](../RELEASE.md)

## Recorded evidence

- Source commit: `76791dd` (`chore(release): prepare TestFlight build 32`).
- User-flow fix: `b07fcfc` (`fix(ios): surface pending plan responses`).
- Branch: `feat/native-ui-design-system`; both commits were pushed before the
  archive was created.
- The archive was built from a clean detached worktree at the exact source
  commit. The main workspace's local plist edit and previous archive artifacts
  were not included.
- OpenAPI client and Xcode project generation produced no source diff.
- Focused Inbox tests passed: 9 tests in 1 suite. Native release configuration
  passed: 31 tests in 8 suites.
- No database migration or backend deployment was required for this native-only
  user-flow release.
- Archive: `SideSeat-Production`, marketing version `1.0.0`, build `32`.
- Bundle ID: `app.sideseat.mobile`; architecture: `arm64`; minimum iOS: `17.0`.
- Embedded API: `https://api.sideseat.de`; environment: `production`.
- Export compliance: `ITSAppUsesNonExemptEncryption=false`.
- SideSeat app binary and dSYM share UUID
  `F6D7A4A1-14D7-3DF3-BE81-4673C5E6EE8D`.
- The guarded Production archive uploaded that SideSeat dSYM to Sentry.
- App Store Connect accepted the upload at `2026-09-07T21:31:32Z`.
- By `2026-09-07T21:34:46Z`, App Store Connect showed build `1.0.0 (32)`
  processed and assigned to the `SideSeat Internal` group, which contained one
  internal tester.

The exporter warned that the prebuilt `Sentry.framework` vendor dSYM was not
inside the archive. This does not replace or invalidate the matching SideSeat
app dSYM recorded above.

## User-flow change

- The Messages tab badge and app icon badge now count visible direct-message
  unread items, pending Plan decisions and pending Outcome answers.
- Saving an Outcome from the Plans history immediately refreshes Inbox state so
  a completed response does not leave a stale attention badge.

## Tonight's minimum acceptance

1. Install or update to TestFlight build 32 and log in with a normal account.
2. Confirm a pending Plan decision or ended accepted Plan produces a Messages
   attention badge.
3. Open the Plans entry from Messages and submit the truthful private Outcome.
4. Confirm the saved state is visible and the attention count decreases or
   disappears after refresh.
5. Confirm the primary Chinese Together → Messages → Plan → Outcome surfaces
   remain Chinese.

Build 30 already passed the full two-account Layer 2 Outcome and Shared
Encounter acceptance. This focused smoke validates the native discoverability
change introduced in build 32.

## Outstanding gates and next step

- Run the five-step focused physical smoke above.
- Then continue recruiting consenting non-QA tester pairs and collect truthful
  bilateral Outcome responses.
- The Layer 2 product Gate remains unpassed. Layer 3 must not start before two
  consecutive qualifying snapshots, with the next formal snapshot no earlier
  than `2026-09-14T10:37:35Z`.

Internal TestFlight distribution is not App Store release approval and does not
close the Layer 2 product Gate.
