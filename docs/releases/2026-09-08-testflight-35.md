# TestFlight release record — 1.0.0 (35)

**Status:** Uploaded, Apple processing complete and available to the existing
`SideSeat Internal` group. Chinese testing notes saved. Production archive and
distribution-payload checks passed. Updating both physical test phones and
accepting the new TestFlight binary remain the next step.

**Date:** 2026-09-08 (Europe/Berlin)

**Scope:** Native matching cancellation hotfix, existing internal TestFlight group only.

## Authority and source

The owner explicitly authorized committing/pushing the matching fix and uploading
a new internal TestFlight version so the test phones can update. This does not
authorize external beta assignment, public submission or production data changes.

- Fix: `a734117`, pushed to `feat/native-ui-design-system`.
- Version/build: `1.0.0 (35)`; refreshed App Store Connect shows 34 as latest
  before preparation.
- Archive source: `594594b9ecefe381e53ead039ca9c03a65b4483c`, pushed.
- Use a clean detached worktree. The main workspace's unrelated `Info.plist`
  encryption-key deletion and untracked Build 29/30 archives remain excluded.
- No backend deployment, configuration change or database migration in this release.
  The existing Build 34 production backend remains in place.

## Verification before packaging

The exact matching implementation being packaged passed the
[matching regression and local two-account acceptance](./2026-09-08-matching-cancellation-regression.md):

- 6 native regression functions / 9 cases passed.
- 8 backend matching integration tests passed, none skipped.
- Real local two-account UI flow passed: both intents and matching sessions →
  separate private YES answers → Plan acceptance → both Calendars.
- Database readback verified the mutual opportunity, bilateral decisions and
  both active Calendar projections. No production test data was written.

Previous simulator acceptance is not acceptance of the new signed TestFlight
binary on two physical phones.

## Executed packaging checks

- `npm run test:release:config`: 66 passed, 0 failed, 0 skipped (31 iOS,
  9 profile, 3 verification, 23 cron/readiness checks).
- `npm run lint` and `npx tsc --noEmit --incremental false`: passed.
- `npm run check:openapi:v1`: all 170 implemented operations match.
- OpenAPI/Xcode project regeneration produced no tracked diff in the release
  worktree. Existing dependency/tool caches were reused; no dependency update.
- Production archive succeeded at approximately `2026-09-08T05:20:25Z`.
  The project's production configuration and mandatory Sentry upload phases passed.
- SideSeat app binary and dSYM UUID both equal
  `D8F6ED62-0238-3CA0-90B2-448443C69220` (arm64); Sentry reported one missing
  debug information file uploaded. The exported app retains the same UUID.
- Strict signature/payload checks passed for the archive at `05:21:04 UTC`
  and the exported App Store IPA at `05:21:47 UTC`: version `1.0.0 (35)`,
  `app.sideseat.mobile`, production API/environment, configured Sentry DSN,
  minimum iOS 17, iPhone-only and `ITSAppUsesNonExemptEncryption=false`.
- The automatically signed archive uses its development provisioning profile;
  App Store export re-signs it correctly. The exported signature AND embedded
  distribution profile have production APNs, Team `V4238R5R53`, correct app
  identity and `get-task-allow=false`. Signed Associated Domains includes
  `applinks:www.sideseat.de`; the profile has no device allowlist.
- Initial local profile validation tried JSON conversion of a provisioning
  profile containing certificate data, which `plutil` cannot represent. The
  read-only verifier was corrected to extract the required fields. The rerun
  passed without changing the binary or signing configuration.
- No backend source, schema, contract or dependency changes from Build 34;
  no backend build/deployment, migration or production-data mutation was performed.
  Public-release readiness is not claimed by these internal packaging checks.

## Upload and distribution

- Apple accepted the upload at `2026-09-08T05:23:31Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED` with exit 0.
- App Store Connect independently showed `1.0.0 (35)` as Processing at
  approximately `05:24 UTC`, then Complete by `05:26 UTC`.
- Build ID: `a5b6f799-f7d7-4a6a-8a90-41dbffa958ed` in SideSeat app
  `6800410369`. The build's group table confirms automatic assignment to
  `SideSeat Internal`, type Internal, one existing tester. No group or tester
  was added. This makes Build 35 available for that group's TestFlight updates;
  it does not prove either phone has installed it.
- Simplified Chinese testing notes were saved successfully, covering the
  cancellation fix, both phones updating to 35, compatible future intents,
  matching-state recovery and the bilateral Plan/Calendar flow. Browser checks
  verified the actual completed upload, group assignment and Saved state,
  following the browser-verification guide through the available CUA connection.
- Apple retains the existing vendor `Sentry.framework` missing-dSYM warning,
  this build's framework UUID `DF0DC4BD-27F3-3B15-954F-4133B56D5746`.
  It did not prevent package acceptance. The separate SideSeat app dSYM upload
  above succeeded; no vendor-symbol or public-quality clearance is claimed.
- No external group, Beta review or public submission was performed.

## Local artifacts

- Archive: `/private/tmp/sideseat-build35.qVYEKA/ios-native/SideSeat-1.0.0-35.xcarchive`
- Validated IPA: `/tmp/sideseat-build35-export/SideSeat.ipa`
- Exported IPA SHA-256: `3ef89401e2ceb93ce54cd41543526d65c7204c81a825db8dcfa4ecc0444b2dc7`
- Logs: `/tmp/sideseat-build35-release-config.log`,
  `/tmp/sideseat-build35-openapi.log`, `/tmp/sideseat-build35-lint.log`,
  `/tmp/sideseat-build35-tsc.log`, `/tmp/sideseat-build35-archive.log`,
  `/tmp/sideseat-build35-export.log`, `/tmp/sideseat-build35-upload.log`,
  `/tmp/sideseat-build35-archive-validation.log`,
  `/tmp/sideseat-build35-ipa-validation.log`.

## Next step

On both test phones, open TestFlight → SideSeat → Update and confirm `1.0.0 (35)`.
Use distinct SideSeat QA accounts with the same activity, overlapping future time
and compatible locations; verify matching → separate private YES answers →
accepted Plan → both Calendars. Record the actual installed versions and result.
The phones were not controlled or updated during this packaging/distribution task.
This internal QA does not pass the organic pilot Gate or authorize a public release.
