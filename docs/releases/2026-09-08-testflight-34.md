# TestFlight release record — 1.0.0 (34)

**Status:** Database migration and backend deployed; Layer 3 enabled for the
internal release. Build 34 processed and available to the existing internal
group. Signed two-account acceptance on Baichu's iPhone now passes through the
repeat Plan's private Outcome and second Shared Encounter, including correction
and restoration. The owner authorized a time-compressed QA window for that last
Plan; this is internal functional evidence, not organic usage or public approval.

**Matching hotfix verified locally (2026-09-08):** The owner reported both phones displaying
`Swift.CancellationError` after Start matching. Targeted simulator-native
cancellation regression reproduces the exact message and loss of loaded matching
cards. The owner-authorized fix now passes 9 native cases, 8 backend tests and
the real local two-account intent → matching → Plan → both Calendars UI flow.
The fix is committed and pushed as `a734117`; replacement internal Build 35 is
uploaded, processed and assigned to `SideSeat Internal`. Phones still on Build 34
must update to receive the fix. See the
[Build 35 release record](./2026-09-08-testflight-35.md) and
[matching regression record](./2026-09-08-matching-cancellation-regression.md).

**Date:** 2026-09-08 (Europe/Berlin)

**Scope:** Layer 3 private Meet Again and Repeat Opportunity, internal beta only.

**Procedure:** [Release](../RELEASE.md)

## Authority and source

The owner authorized pushing the pending commits, deploying the reviewed database
migration and backend, then uploading a new internal TestFlight build and
performing signed two-account acceptance. This does not authorize App Store
publication or external beta distribution. The owner subsequently made
Baichu's iPhone available explicitly for this signed-device acceptance session.

- Feature source: `f392d90`, pushed to `feat/native-ui-design-system`.
- Release source: `0a59d5e` (`chore(release): prepare TestFlight build 34`).
  All five previously pending commits and this build preparation are pushed.
- Build 34 uses a clean release worktree; the main workspace's unrelated plist
  edit and old archives are excluded.
- Local Layer 3 evidence: [internal acceptance](../LAYER3_INTERNAL.md).
- The real non-QA product-data Gate remains open and independent of development.

## Database and backend

- Before migration, created Neon data-and-schema branch
  `pre-layer3-20260908-0a59d5e` (`br-super-dust-apsx56sw`) from production `main`
  in project `hidden-star-16421165`, at approximately `2026-09-08T02:39Z`.
  The console confirmed seven-day expiry at `2026-09-15T02:39Z` (minute precision).
  Production's configured history window is only six hours; the seven-day
  release branch is a separate recovery point, not seven-day PITR coverage.
  No production restore or promotion of the backup branch was performed.
- Applied the sole pending additive migration
  `20260908010000_meet_again_repeat` with a one-command remote-migration opt-in.
  The exact production endpoint/database were asserted before execution.
  `prisma migrate status` then reported `Database schema is up to date!`.
  The remote-migration opt-in was not persisted in Vercel.
- Deployed the clean source archive as production-target candidate with
  `--skip-domain`, migration execution skipped and `V2_MEET_AGAIN_ENABLED=0`.
  Candidate `dpl_5J6sWWTUyMFSi18u3k5vKDi4UADj` became Ready at
  `2026-09-08T02:44:40Z`, using Next.js `15.5.23`; remote build took about four
  minutes and completed without build errors.
- After candidate smoke passed, promoted it to `https://api.sideseat.de` and
  the existing production aliases. Candidate URL:
  `https://sideseat-cplxjactb-baichus-projects.vercel.app`.
- Two existing QA accounts passed native login, private Plan reads, Weekly
  Intent reads, Messages inbox and Calendar schedule reads on the candidate
  and again on the production domain by `2026-09-08T02:47:50Z`. Each newly
  created smoke session was individually revoked. Private Plan DTOs exposed
  only viewer answers, with Meet Again unavailable while disabled. The new
  unauthenticated Meet Again route returned `401 AUTHENTICATION_REQUIRED`.
- Production client-config reports production APNs and the existing Together
  flags enabled. Both API/www AASA endpoints return the expected signed app
  identity; public privacy/support pages return 200. This is configuration and
  HTTP evidence, not real push delivery or Universal Link device acceptance.
- The initial deployment-scoped error-log query returned no matching logs;
  this short observation is not continuous monitoring certification.
- After Build 34 became available internally, verified the same source with
  `V2_MEET_AGAIN_ENABLED=1` on a separate production-target candidate:
  `dpl_Fe5VGrTs3V6p7vtHmAQ87MMMqP8j`,
  `https://sideseat-hj6lc4g59-baichus-projects.vercel.app`.
  It became Ready at `2026-09-08T02:51:25Z`, after a roughly two-minute remote
  build. Both QA accounts passed candidate smoke by `2026-09-08T02:52:56Z`.
  No domains were switched before that smoke and internal-client readiness.
- Added the production `V2_MEET_AGAIN_ENABLED=1` setting, promoted that enabled
  candidate, then repeated both QA accounts' HTTP smoke on `api.sideseat.de`
  successfully by `2026-09-08T02:53:18Z`. Client-config reports `v2MeetAgain=true`;
  each Plan's private entry eligibility matches the viewer's own Outcome and
  ended/accepted state. No bilateral answer arrays are exposed. The enabled
  deployment's bounded error-log query returned no matching errors.
- Immediate compatible rollback: set the production feature setting back to
  `0` and promote the verified feature-off deployment
  `dpl_5J6sWWTUyMFSi18u3k5vKDi4UADj`. Changing project env alone does not change
  already-running deployment configuration. Do not drop consent/history tables
  or modify Outcome answers. The earlier Layer 2 backend
  `dpl_HWM19p9xx9pHQQgc9CAAidoa4nkH` is retained but is not the preferred rollback.

Production readiness limits remain explicit: Vercel's local env pull omits
sensitive values, so running the full readiness checker against that pull fails
and cannot certify server credentials. Provider presence and live API checks
confirm configured production dependencies, but `CRON_MONITOR_URLS`,
`DATABASE_BACKUP_PROVIDER`, `DATABASE_BACKUP_RETENTION_DAYS` and
`DATABASE_RESTORE_TESTED_AT` are still absent from production configuration.
No monitoring URL or restore-drill date was invented. Do not report the full
public-release go/no-go gate as passed. No log drain was added or certified.

## Archive, symbols and distribution

- `npm run test:release:config`: 66 tests passed (31 native release, 9 profile,
  3 school verification and 23 cron/readiness checks); zero failures.
  Lint, TypeScript and OpenAPI route coverage passed. OpenAPI/Xcode generation
  in the clean worktree produced no tracked source diff.
- A later check in the **main dirty workspace**, not the release worktree,
  passed 30/31 native checks and failed the export-encryption declaration
  assertion because the owner's pre-existing `Info.plist` edit removes
  `ITSAppUsesNonExemptEncryption=false`. That unrelated edit is preserved,
  unstaged and excluded from this release. Do not call the main-workspace run
  green or build the next release from it without resolving that declaration.
- Production archive succeeded; bundle `app.sideseat.mobile`, version/build
  `1.0.0 (34)`, arm64, minimum iOS 17, iPhone-only, production API
  `https://api.sideseat.de`, `ITSAppUsesNonExemptEncryption=false`.
- Archive binary/dSYM UUID: `F3D4EBBC-17FC-3E36-88A2-F0A2F17867DF`.
  The guarded archive uploaded that matching SideSeat dSYM to Sentry.
- Archive and separately exported App Store IPA passed strict code-signature
  verification. Exported signature and profile both contain production APNs,
  Team `V4238R5R53`, correct application identity and `get-task-allow=false`.
  Signed Associated Domains contains `applinks:www.sideseat.de`.
- Apple accepted the upload at `2026-09-08T02:46:42Z`; Xcode reported
  `Upload succeeded` and `EXPORT SUCCEEDED`. App Store Connect independently
  showed Build 34 processing. Existing vendor `Sentry.framework` dSYM warning
  remains; it did not prevent Apple accepting the package, and does not
  negate the successful matching SideSeat app dSYM upload above.
- By `2026-09-08T02:51Z`, Apple processing was complete and Build 34
  (`709a6829-d23c-40bf-a3ba-39275604988f`) was automatically assigned to the
  existing `SideSeat Internal` group with one tester. Chinese testing notes
  were saved for the first/repeat flow, private consent, truthful Outcome
  answers and the separation of QA from organic pilot evidence.
- At `2026-09-08T02:52Z`, the group's tester list reported
  `Installed 1.0.0 (34)`. This is Apple's install-status telemetry, not a
  physical-device version probe or completed two-account acceptance.
- No new testers/groups were added, and no Build 34 external group, Beta
  review or public submission was performed. Build 33's existing external
  review remained Waiting for Review; it does not contain Layer 3.

Local archive: `/private/tmp/sideseat-build34.bD59Hp/ios-native/SideSeat-1.0.0-34.xcarchive`.
Validated IPA: `/tmp/sideseat-build34-export/SideSeat.ipa`.
Logs: `/tmp/sideseat-build34-archive.log`, `/tmp/sideseat-build34-export.log`,
`/tmp/sideseat-build34-upload.log`, `/tmp/sideseat-build34-api-smoke-staged.log`,
`/tmp/sideseat-build34-api-smoke-production.log`,
`/tmp/sideseat-build34-api-smoke-enabled-staged.log`,
`/tmp/sideseat-build34-api-smoke-enabled-production.log` and
`/tmp/sideseat-layer3-production-migration-status.log`.

## Signed-device acceptance — 2026-09-08

The owner explicitly authorized Baichu's iPhone for this session. It is an
iPhone 16 Pro Max running iOS 26.0.1 (23A355), using the Chinese interface.
The device initially still had Build 33 despite Apple's earlier installation
telemetry. Updated through TestFlight's **Update** button; TestFlight then showed
**Open** for `1.0.0 (34)`. A separate CoreDevice installed-app query confirmed
`app.sideseat.mobile`, `1.0.0 (34)` at `2026-09-08T03:01:45Z` and again after
the core acceptance. The installed SideSeat binary was not replaced with a
development build. A separately signed XCTest host drove the installed app
without DEBUG fixture arguments against `https://api.sideseat.de`.

The acceptance followed device UI → production HTTP → scoped read-only database
verification → the other account's UI. Only the two existing internal QA accounts
were used. Their synthetic answers are acceptance fixtures, not evidence that
real users met. Database inspection used an explicitly read-only transaction
after asserting the production endpoint. No SQL writes, backdating, fabricated
organic answers, or changes to other accounts' Plans were performed.

| Check | Observed result |
| --- | --- |
| Own Outcome and permission | A selected OCCURRED, saved YES, withdrew, selected NO, then saved YES again; YES persisted after app relaunch. |
| Unilateral privacy | Before B answered, B still had its own unanswered controls and did not inherit A's saved permission. A-only database snapshot had one Outcome and no Shared Encounter. |
| Shared Encounter boundary | After B independently selected OCCURRED, the source QA Plan had two OCCURRED rows and one Shared Encounter. Both permissions were YES; no new Opportunity existed yet. |
| Fresh matching inputs | Both accounts created new COFFEE Intents after the source Plan ended and explicitly started matching. Both selected `2026-09-08T03:45Z–04:15Z`; no other active COFFEE matching intents were present in the preflight/readbacks. |
| New repeat source | A new PENDING Opportunity had `isRepeat=true`, the latest ended QA Plan as internal provenance, a new source ID and zero decisions. It was not a reopened old Opportunity. |
| New bilateral consent | B's new YES remained PENDING with one decision and no source message. A still had to select its own new YES; only then did the Opportunity become MUTUAL and create a new source message in the existing canonical chat. |
| Plan creation | The new source card opened the Plan sheet with repeat details prefilled. Sending created one new QA Plan carrying the new Opportunity origin/snapshot. |
| Acceptance and Calendars | B accepted that Plan. B and then A each opened it in the installed app's Calendar; the continuation test passed at `2026-09-08T03:13:40Z`. Readback confirmed ACCEPTED and two distinct new participant Calendar entries, separate from the old Plan's entries. |
| Production DTO privacy | Both accounts' authenticated Plan reads returned viewer-only Outcome/Meet Again values; no raw bilateral answer arrays, counterpart-answer fields or permission count were exposed. Temporary HTTP smoke sessions were individually revoked. |
| QA cleanup | Stopped A's matching at `03:14:45Z` and B's at `03:15:26Z`, without canceling the accepted Plan or removing history. The `03:16:04Z` readback confirmed both stops; the device cleanup test passed and left the first QA account signed in. |
| Pilot exclusion | `npm run test:layer2-pilot`: 3 passed, including repository-owned QA exclusion, silence handling and all-threshold Gate logic. No formal organic snapshot was run. |

### Harness issue and evidence limits

One device test stopped on a **test-harness title assertion**, not a failed Plan
save: simulated cursor/deletion left the prefilled `咖啡` suffix, producing
`[build34-qa] repeat acceptance咖啡` instead of the expected exact title. Readback
confirmed the actual PENDING Plan and correct new origin. Continued from that
same persisted Plan using its observed ID/title; did not repeat permissions,
new consent, or Plan creation. Its receiver acceptance and both Calendar views
then passed. Do not report the original failing test as green.

That failed run's Xcode diagnostic finalization subsequently stalled and its
own process was terminated after the separate continuation completed; its log
is retained, but its incomplete result bundle is not a complete evidence bundle.
An earlier fresh probe build also required the existing local clang wrapper
after toolchain discovery stalled. Neither issue required a SideSeat source fix
or a replacement app build.

At this initial Calendar checkpoint, the new Plan's original end was
`2026-09-08T04:15Z`, with no Outcome answers or second Shared Encounter yet.
The subsequent owner-authorized time-compressed signed-device closure is recorded
below and supersedes that pending status. Real APNs delivery, oldest supported
iOS, production Sentry event capture and outstanding monitoring/restore
requirements are still not certified by this run.

Successful device result bundles:

- `/tmp/SideseatBuild34InstallAndVersion.xcresult`
- `/tmp/SideseatBuild34FirstPrivateOutcome.xcresult`
- `/tmp/SideseatBuild34SecondPrivateOutcome.xcresult`
- `/tmp/SideseatBuild34FreshIntents.xcresult`
- `/tmp/SideseatBuild34RepeatFirstDecision.xcresult`
- `/tmp/SideseatBuild34RepeatPlanCalendarContinuation.xcresult`
- `/tmp/SideseatBuild34QACleanup.xcresult`

Supporting logs: `/tmp/sideseat-build34-repeat-plan-calendars.log` (the failed
harness assertion), `/tmp/sideseat-build34-private-dto-final.log`,
`/tmp/sideseat-build34-pilot-exclusion-tests.log`,
`/tmp/sideseat-build34-readback-assertions.log` and the scoped
`/tmp/sideseat-build34-db-*.log` readbacks. The probe source is local-only at
`/private/tmp/sideseat-testflight-probe.euKGf0/Tests/TestFlightProbeUITests.swift`.
Screenshots were exported alongside the result bundles; key private-state,
fresh-repeat and both Calendar screens were visually checked. Some raw captures
include unrelated notification banners: keep them local and do not publish
those captures or QA credentials. Temporary artifacts are not permanent storage.

## Outcome closure — owner-authorized time-compressed QA

The owner explicitly requested completing the remaining signed-device Outcome
flow on 2026-09-08. This expands acceptance beyond the Calendar checkpoint above,
not release/distribution authority. At `2026-09-08T03:19:33Z`, the same QA Plan
remained ACCEPTED, with no Outcome answers or derived Shared Encounter and an
unchanged end time of **`2026-09-08T04:15:00Z` / 06:15 Europe/Berlin**.

The installed Build 34 pre-end read-only UI check passed at `03:23:58Z`: the
accepted Plan was visible but its Outcome and Meet Again actions were absent.
The independent `03:24:08Z` database/private-DTO preflight passed for both QA
accounts, with both matching sessions still stopped and both Calendar entries
preserved. No Outcome mutation was attempted before the real end time.

Pre-end evidence: `/tmp/SideseatBuild34RepeatPreEnd.xcresult`,
`/tmp/sideseat-build34-repeat-pre-end.log`,
`/tmp/sideseat-build34-repeat-outcome-before-check.log`. The new closure methods
were compiled as part of that probe run, before after-end acceptance began.

### Authorized QA time fixture

The owner subsequently explicitly allowed moving only this existing QA Plan
and its two linked Calendar time windows into the past, to complete acceptance
without waiting for the original end time. This supersedes this session's prior
no-backdating instruction for these three rows only; it is not general database
write authority. At `2026-09-08T03:43:56.332Z`, one guarded transaction changed
only the Plan's `startTime`/`endTime` and the two Calendars' `startAt`/`endAt`:

- Original window: `2026-09-08T03:45:00Z`–`04:15:00Z` (05:45–06:15 Berlin).
- QA fixture window: `2026-09-08T03:10:00Z`–`03:40:00Z` (05:10–05:40 Berlin).
- Exactly one Plan and two Calendar rows changed. Their identities, ownership,
  stable commitment, accepted status and all other columns were preserved.
- Original source Plan/Calendars/answers/permissions/Shared Encounter, new
  Opportunity and its immutable source snapshot, and both fresh Intents were
  compared unchanged. No Outcome, permission or Shared Encounter was written.

The exact Neon host/database, QA usernames/title/location, pinned Plan/Calendar
IDs, original times and unanswered state were checked first. The local original
snapshot is `/tmp/sideseat-build34-time-fixture-original.json`; the committed
transaction receipt is `/tmp/sideseat-build34-time-fixture-applied.json`. An
initial preflight assumption about the commitment was rejected before any write;
the actual existing stable commitment was then checked and preserved.

The phone was rechecked as `1.0.0 (34)`. Neither SideSeat's app binary, product
code, server/device clocks nor the server's ended-Plan rule was changed. Only
the local probe/checker's expected QA time was updated. Subsequent answers must
still be submitted through the installed TestFlight UI and derived by production
services. This is **synthetic, time-compressed functional QA**, not natural
passage of time or evidence of real meetings. It must remain excluded from the
organic Gate. Never copy QA credentials or notification-bearing captures to Git.

The **read-only** phase `00` check passed at `2026-09-08T03:44:06.331Z`, after
the authorized fixture end time, with both answers and the new encounter absent:

```sh
node --env-file=.env /tmp/sideseat-build34-repeat-outcome-check.mjs 00
```

### Completed signed-device Outcome closure

Completed by `2026-09-08T04:04:23.539Z`. All eight answer writes came from the
installed TestFlight UI. Each state was checked in a read-only production
transaction and in both viewer-only HTTP Plan DTOs; the checker created/revoked
only its own short-lived QA authentication sessions. The two probe corrections
and the throttled phase 05 readback are retained below, not reported as green.

| Phase | Device entry / action | Actual QA answers A / B | Repeat Shared Encounter | Successful state read UTC, 2026-09-08 |
| --- | --- | --- | --- | --- |
| 00 | Read-only baseline | unanswered / unanswered | absent | 03:44:06.331 |
| 01 | A: Together → no; read-only UI continuation | DID_NOT_OCCUR / unanswered | absent | 03:46:03.260 |
| 02 | A: Calendar → detail → Plan history → skip | PREFER_NOT_TO_SAY / unanswered | absent | 03:50:59.501 |
| 03 | A: Messages history → occurred | OCCURRED / unanswered | absent | 03:52:29.503 |
| 04 | B: Together → skip; starts with own unanswered state | OCCURRED / PREFER_NOT_TO_SAY | absent | 03:53:39.528 |
| 05 | B: Calendar → detail → Plan history → no | OCCURRED / DID_NOT_OCCUR | absent | 04:00:14.559 |
| 06 | B: Messages history → occurred | OCCURRED / OCCURRED | exactly one | 04:01:23.745 |
| 07 | B: Plans → change own answer to skip | OCCURRED / PREFER_NOT_TO_SAY | removed | 04:02:08.623 |
| 08 | B: Plans → restore own occurred answer | OCCURRED / OCCURRED | exactly one | 04:02:54.937 |
| 09 | Both accounts: relaunch/read only; leave A signed in | OCCURRED / OCCURRED | same record as phase 08 | 04:04:23.539 |

Phase 01's corrected read-only UI continuation passed at `03:47:22Z`; its write
was not replayed. Every answer phase verified saved/edit state after relaunch.
Both accounts' saved OCCURRED state exposes fresh, unanswered Meet Again choices,
not the prior encounter's YES permission or the other participant's response.

The repeat encounter was first derived at `04:01:01.815Z`, removed in phase 07,
and newly derived at `04:02:33.087Z` after restoration. Phase 08/09 record IDs and
timestamps were compared equal: read/relaunch did not duplicate or rederive it.
Only the pinned source and repeat Plans were counted, ending with two encounters;
unrelated older QA encounters were not mistaken for this repeat.

The original snapshot comparison at `04:03:21.906Z` also passed: original source
Plan, Calendars, Outcome/permission/encounter records, immutable Opportunity and
Intents were unchanged. The target commitment and two Calendar identities stayed
unchanged, the authorized QA times persisted, and target Meet Again permissions
remained empty. Both matching sessions stayed stopped. Evidence:
`/tmp/sideseat-build34-repeat-outcome-final-integrity.log`.

Successful closure bundles (nine; separate from the two failed attempts):

- `/tmp/SideseatBuild34RepeatOutcome01ReadOnly.xcresult`
- `/tmp/SideseatBuild34RepeatOutcome02CalendarRetry.xcresult`
- `/tmp/SideseatBuild34RepeatOutcome03.xcresult` through
  `/tmp/SideseatBuild34RepeatOutcome09.xcresult`

State logs are `/tmp/sideseat-build34-repeat-outcome-00.log` through `09.log`,
except phase 05's successful log is `05-after-reset.log`. Seventeen screenshots
were exported under `/tmp/sideseat-build34-repeat-outcome-evidence-01` through
`-09`; key Chinese saved no/skip/occurred, B's independent unanswered state,
correction and both final private permission screens were visually checked.
These are local QA artifacts, not public store assets or permanent storage.

The existing separate `TestFlightProbe` project and clang wrapper were used;
only that test host was built, never a replacement SideSeat app. Do not rerun
these mutating phases from zero: the pinned QA Plan is already answered.
`npm run test:layer2-pilot` passed all three exclusion/coverage contract tests;
documentation whitespace and relative-link checks passed. No product fix, new
build, migration, deployment, push or distribution was needed for this closure.

### Probe corrections and scheduling

Two closure probe runs failed, separately from the earlier Plan-title issue:

- Phase 01 submitted DID_NOT_OCCUR successfully, then required a non-interactive
  saved-answer label to be hittable. Production readback confirmed the actual
  saved value and no encounter. The corrected probe checks the real edit button
  plus label existence; a read-only continuation passed without replaying the
  write. Retained failure: `/tmp/sideseat-build34-repeat-outcome-device-01.log`.
- Phase 02 initially tried the future-Plan Calendar shortcut, which is replaced
  by Outcome controls on ended Plans. That attempt submitted no answer. After
  verifying phase 01 was unchanged, the probe entered the existing event directly
  from the Calendar tab and completed Calendar → detail → Plan history → Skip.
  Retained failure: `/tmp/sideseat-build34-repeat-outcome-device-02.log`.

These failures were test-harness assumptions, not SideSeat code changes. Their
Xcode diagnostic finalization stalled after reporting test failure; only those
two failed-run processes were terminated. Logs remain available; do not treat
their incomplete result bundles as successful evidence.

After phase 05's device test passed, its extra HTTP readback login returned
`429 RATE_LIMITED`: the probe/checker had repeatedly logged the same QA accounts
in. The existing contract permits 10 account/IP logins in a fixed 15-minute
window. The sequence stopped before phase 06. No rate counters or limits were
changed, and no IP/header workaround was used. The native probe was narrowed
to reuse its current session after checking the exact visible QA username;
switching accounts still uses normal logout/login. The failed checker log is
`/tmp/sideseat-build34-repeat-outcome-05.log`. After the normal
`2026-09-08T04:00:00Z` reset, the read-only phase 05 retry passed for both accounts
at `04:00:14.559Z`, without replaying the UI write. Successful retry evidence:
`/tmp/sideseat-build34-repeat-outcome-05-after-reset.log`. The initial 429 remains
recorded as test-traffic throttling, not a successful HTTP check.

The earlier separate 06:16 one-off heartbeat request was rejected because this
task already has the weekly heartbeat; no follow-up was created. The authorized
time fixture makes that proposed follow-up unnecessary. No automation was
created, modified or deleted during this closure run.

## Next step

Signed Build 34 acceptance is complete through the repeat Plan's Outcome and
second Shared Encounter, including correction/restoration and both private
relaunch readbacks. This final Plan used the explicitly authorized synthetic time
fixture; it does not prove an organic meeting. Next, push the scoped acceptance
documentation commits when authorized. Request approval before assigning
Build 34 to external TestFlight testing or submitting Beta review; this session did not
authorize or perform either action or App Store publication. Build 33 does not
contain Layer 3.

The owner clarified that only internal QA is running; the real-user small-group
pilot has not launched. Its product-data Gate remains open for later authorized
collection, with the existing earliest formal snapshot `2026-09-14T10:37:35Z`;
these QA answers, Intents and the new Plan must remain excluded. No weekly
automation has been modified here. Complete outstanding
public-release monitoring/recovery and device requirements before claiming
public readiness.
