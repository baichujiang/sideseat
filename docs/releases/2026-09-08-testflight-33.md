# TestFlight release record — 1.0.0 (33)

**Status:** Core-flow smoke and focused simulator Light/Dark UI acceptance passed

**Date:** 2026-09-08 (Europe/Berlin)

**Scope:** Unified Together and Plan user flows, cards and native sheets

**Governing procedure:** [Release](../RELEASE.md)

## Source and verification

- Source commit: `6a1c2f7` (`chore(release): prepare TestFlight build 33`).
- Feature commit: `ab9fcd9` (`feat(ios): unify Together and Plan user flows`),
  pushed to `feat/native-ui-design-system` before release preparation.
- Build from a clean detached worktree at the exact source commit. The main
  workspace's local plist edit and previous archives are not included.
- OpenAPI client and Xcode project generation produced no source diff.
- Native release configuration: 31 tests in 8 suites passed for this source.
- The feature revision passed 6 state tests and 7 UI tests, including
  Light/Dark, largest Dynamic Type, Plan acceptance, Calendar navigation,
  Intent editing and private Outcome editing. See the
  [design verification](../DESIGN_SYSTEM.md#together--plan-verification--2026-09-08).
- No database migration or backend deployment is required for this native-only
  release. No production Outcome data was created or modified for release checks.

## Archive and distribution

- Production archive succeeded on 2026-09-08 (Europe/Berlin).
- Bundle ID: `app.sideseat.mobile`; version/build: `1.0.0 (33)`;
  architecture: `arm64`; minimum iOS: `17.0`; iPhone only.
- Embedded API: `https://api.sideseat.de`; environment: `production`.
- Export compliance: `ITSAppUsesNonExemptEncryption=false`.
- App binary and dSYM share UUID
  `770B6EFC-CE9D-3A0E-8BB0-7D577C1D225C`.
- The guarded Production archive uploaded that exact SideSeat dSYM to Sentry.
- Archive code-signature verification passed. The automatic-signing archive
  uses a development profile; App Store export re-signs it for distribution.
- A local App Store export succeeded before upload. The exported IPA passed
  strict signature verification, retained build `33` and the production API,
  and its signed entitlements and embedded profile both contain production
  APNs, the correct Team/bundle identity and `get-task-allow=false`.
  Signed Associated Domains includes `applinks:www.sideseat.de`.

- App Store Connect accepted the upload at `2026-09-07T22:52:32Z`
  (`2026-09-08 00:52:32` Europe/Berlin); Xcode export/upload succeeded.
- The exporter reported the existing missing prebuilt `Sentry.framework`
  vendor dSYM warning. The matching SideSeat app dSYM above uploaded
  successfully; the vendor warning did not block Apple accepting the build.

- By `2026-09-07T22:56:23Z`, App Store Connect showed processing complete and
  build `1.0.0 (33)` automatically assigned to `SideSeat Internal`, containing
  one internal tester. No new testers or groups were added.
- Chinese TestFlight testing instructions were saved for the Together editor,
  Plan acceptance/Calendar path, truthful private Outcome answers and
  Light/Dark/large-text inspection.
- No external beta review or App Store submission was performed.

TestFlight installation and the focused physical core-flow smoke below are
verified for build 33. A new real-event symbolication check is not yet verified.

## Changes for testers

- Together prioritizes delivered opportunities and offers a direct Plans entry.
- Weekly Intent editing has two steps: activity, then available times. Going
  back preserves the draft; saving does not start matching automatically.
- Together and Plan surfaces share a consistent card, status and action layout.
- Plan creation and rescheduling use a native sheet with a pinned submit action.
- Accepted historical Plans show private Outcome choices. A saved answer stays
  compact and can be changed; no answer is inferred from silence.
- Shared Encounter still requires both participants to report `OCCURRED`.

## Physical acceptance — 2026-09-08 (Europe/Berlin)

- Device: iPhone 16 Pro Max, iOS 26.0.1, Chinese UI. Both CoreDevice's installed
  app metadata and the TestFlight details screen confirm `1.0.0 (33)`.
- A separate signed XCTest probe drove the already-installed TestFlight app.
  It did not rebuild or replace SideSeat with a development/test-fixture build.
- Two existing internal QA accounts were exercised sequentially on this one
  device; this was not a simultaneous two-device test.

| Focused path | Observed result |
| --- | --- |
| Together → Intent activity → available times → back → save | Passed. Draft retained, date controls and pinned save available, saved Intent visible; matching did not start automatically. |
| Existing mutual chat → Plan sheet → send | Passed. Title/location entry and pinned submission worked with the keyboard shown. |
| Other account → received Plan → accept → Calendar | Passed. Plan became confirmed and its Calendar entry was observed on the receiving account. |
| Relaunch/account switch → proposer Calendar | Passed. The confirmed Plan and matching Calendar entry persisted for both accounts. |
| Historical own Outcome → compact saved state → Change answer | Passed on both accounts. Three choices were exposed, including did-not-occur and skip; no choice was submitted. |
| Largest accessibility text → unsaved Intent draft → time step | Passed. The field was reachable by scrolling, and the pinned save remained enabled/hittable with a target at least 44 points high. No extra Intent was saved. |
| Light/Dark cards and native sheets | Screenshots visually inspected in both appearances, including date controls. The complete automated navigation sequence remains pending because system notification banners repeatedly interrupted it. |

Data scope: this run saved one explicitly internal QA Intent and created and
accepted one explicitly QA Plan. Matching was not started. These are not real
pilot samples. **Zero Outcome answers were created or changed.** Historical
saved answers already existed before build 33; this run does not claim a new
bilateral `OCCURRED` transition or new Shared Encounter derivation.

The focused two-account readback test passed in
`/tmp/SideseatBuild33ReadbackAndPresentation.xcresult`. Creation, acceptance and
the first Calendar observation were captured in earlier staged runs:
`/tmp/SideseatBuild33CoreFlow.xcresult` and
`/tmp/SideseatBuild33AcceptanceFinal.xcresult`. The version assertion also passed
in `/tmp/SideseatBuild33PlanAcceptance.xcresult`.

Largest accessibility text and the exact TestFlight version assertion passed in
`/tmp/SideseatBuild33VisualFinal.xcresult`. The large-text check used the standard
UIKit preferred-content-size launch override on the installed app; its screenshot
visually confirms enlarged text. It did not change the device's global text-size
setting, and the app was relaunched without the override afterward. The combined
run still failed its separate Light/Dark navigation test when a system banner
intercepted Back; the captured hierarchy remained on the time step. Do not report
that combined result bundle as a fully passing suite.

The last presentation-only rerun,
`/tmp/SideseatBuild33PresentationVerified.xcresult`, also failed: after capturing
the light Plan sheet/date controls, a system banner remained present through
the probe's eight-second dismissal wait. At the owner's request, stop physical
UI automation and use an isolated SideSeat simulator for the remaining visual
regression. Do not require the owner's phone or Do Not Disturb for that work. The
artifact name does not indicate a passing result. Physical Light/Dark screenshots
from the earlier staged runs remain observational evidence, not a green suite.

Those earlier aggregate runs were **not all green**: the probe assumed an empty
navigation stack, encountered system notification/AX interruptions, used an
incorrect TestFlight version selector, and attempted to restore an unsupported
`.unspecified` XCTest appearance. The first largest-text attempt also tried to
type into a field below the visible sheet area. Probe-only corrections followed
the observed failures; no product code was changed. The initial dark appearance
was restored after Light/Dark inspection. No new SideSeat crash appeared in the
device crash-file listing checked during the AX investigation; this is not a
Sentry symbolication check.

Local screenshot evidence is retained under `docs/visual-qa/testflight-33/`
(Git-ignored); raw XCTest artifacts can contain system notifications and must
not be published as tester screenshots.

### Scope limits and follow-up

- Non-blocking localization observation: the Chinese chat's more-actions icon
  still exposes the English VoiceOver label `Chat actions`. Fix that label in a
  subsequent native change; no visible primary-flow blocker was observed.
- Fresh, truthful Outcome submission, badge refresh after that submission and
  a new Shared Encounter transition were not exercised in this run. Prior
  Layer 2 contract evidence is recorded separately; it is not new build-33
  execution evidence.
- This focused smoke does not re-certify production APNs, real-event Sentry
  symbolication, oldest-supported-iOS coverage or App Store release readiness.
- No new testers/groups, external beta review or App Store submission were
  performed. Distribution remains the existing one-tester internal group.

## Next step

1. Complete Beta App Information and reviewer contact/login information with
   owner-provided details, then obtain authorization to submit Build 33 for
   external Beta App Review. Reuse the existing `SideSeat 用户测试` group;
   confirm approval and external installation before distributing invitations.
   See the access readiness check below. The release/acceptance commits through
   `cd3ebda` have been pushed; the simulator fixtures do not certify production
   Outcome behavior.
2. Continue real Plan use and collect both participants' truthful private
   Outcome answers after the activity. Verify saved state and badge refresh;
   a new Shared Encounter may appear only after both truthfully report
   `OCCURRED`. Do not turn the QA fixtures above into pilot evidence.

Then continue the real non-QA Layer 2 pilot. The Layer 2 Gate remains open and
Layer 3 remains blocked until two consecutive qualifying snapshots at least
seven days apart. The next formal snapshot is no earlier than
`2026-09-14T10:37:35Z`. See the [pilot contract](../LAYER2_PILOT.md).

Internal TestFlight distribution is not external beta review, App Store release
approval, or a passed Layer 2 product Gate.

## Simulator-only follow-up — 2026-09-08

The initial attempts below were interrupted. The subsequent recovery and
passing run are recorded in the next subsection; the earlier result bundles
remain interrupted evidence, not passing suites.

The owner is using the physical phone for another app. No physical-device
commands or UI actions were performed in this follow-up. Iterative visual checks
remain simulator-only; any future necessary physical session must be arranged
explicitly. The release procedure now records that default.

- Built the exact release source `6a1c2f7` in the detached worktree, using the
  `SideSeat-Development` scheme. The simulator app plist reports `1.0.0 (33)`.
  This is a Development simulator build, not the installed TestFlight binary.
- Selected only `testTogetherFlowEditorLightAndDark` and
  `testPlanFlowComposerAndOutcomeLightAndDark`. Their authenticated UI fixtures
  bypass production Plan/Outcome writes; they cannot contribute to the pilot.
- The newly created dedicated simulator stayed at its initial system boot
  progress screen. That attempt was stopped before test execution. The build
  products were reused on the existing, previously shut-down
  `SideSeat Composer Preview 20260813` (iPhone 17 Pro, iOS 26.5).
- On that simulator, the light Plan list, response card and prefilled
  alternative-time sheet were captured and visually inspected. The pinned
  submission was exercised. The test then stalled on app relaunch; its log
  includes `DebuggerVersionStore.StoreError` / `no debugger version`. This is
  an observed launch stall; the metadata warning alone does not establish an
  application defect.
- No selected test case completed. Together and the full Light/Dark sequence
  must not be reported as newly passing. Direct simulator launch and install
  also remained pending after a simulator restart; those attempts were stopped.
- No business code changed, no production Plan/Outcome records were written,
  and no production deployment or TestFlight upload was performed. Only the
  SideSeat-specific simulator/jobs were stopped; the owner's phone and other
  simulator sessions were left alone.

Evidence: `/tmp/sideseat-build33-simulator-acceptance.log`,
`/tmp/sideseat-build33-simulator-ui.log`, and the interrupted result paths
`/tmp/SideseatBuild33SimulatorAcceptance.xcresult` and
`/tmp/SideseatBuild33SimulatorUI.xcresult` (not passing result bundles).
The three inspected `flow-plan*.png` images are retained in the release
worktree's `docs/visual-qa/` and copied to the main workspace's Git-ignored
`docs/visual-qa/testflight-33-simulator/`.

### Recovery and passing acceptance — 2026-09-08 00:27 UTC

- Used only `SideSeat Build33 UI Acceptance` (iPhone 17 Pro, iOS 26.5),
  device `DA2735F2-9D4B-40C2-AD18-AA88E537A40C`. No physical-device actions
  or changes to other simulator sessions were made.
- `simctl bootstatus` exposed an unfinished CoreLocation data migration.
  Process samples showed its synchronous wait through `locationd` to a stuck
  `dasd`. Restarting only this simulator's `dasd` released the migration;
  boot completed at `2026-09-08T00:22:29Z`. No runtime files, permission
  settings or product code were changed. Do not infer that this proves the
  cause of every earlier launch stall.
- Reused the exact release-source build (`6a1c2f7`, Development, `1.0.0 (33)`)
  and ran the two unchanged selected tests. At `2026-09-08T00:27:34Z`,
  Xcode reported **TEST SUCCEEDED: 2 tests, 0 failures, 0 skipped**.
  Plan/Outcome took 113.838 seconds; Together took 36.015 seconds.
- Plan, in both appearances: Messages → pending Plans → response card →
  prefilled alternative-time sheet → pinned submit → successful dismissal;
  relaunch → historical Plan → occurred → compact saved state → edit →
  did-not-occur → saved state. Skip was visible but not submitted in this run.
- Together, in both appearances: opportunity card → add Intent → Chinese
  activity input → available-time step → enabled/hittable pinned save → back
  with the activity draft preserved. This selected test does not submit the
  Intent; the earlier physical smoke separately verified saving it.
- Captured and visually inspected all 14 `flow-*.png` screenshots. Chinese
  action/status text, date controls, card hierarchy and pinned sheet actions
  were readable; keyboard-visible Intent progression remained reachable.
  English activity/course/location strings in the fixtures represent content,
  not a claim that all app localization has been audited. The previously
  recorded `Chat actions` VoiceOver follow-up remains open.
- All Outcome changes stayed in the authenticated DEBUG fixture branch;
  **zero production Outcome writes and zero non-QA pilot samples**. This
  does not verify bilateral server derivation, production badge refresh,
  APNs or a new Shared Encounter. Layer 3 remains blocked by the pilot Gate.
- `npm run test:ios-release:config` also passed: 31 tests in 8 suites.
  No new build/upload or production deployment was necessary.

Passing evidence: `/tmp/SideseatBuild33SimulatorRecovered.xcresult`
(`xcresulttool` summary: `Passed`, two passed, zero failed/skipped),
`/tmp/sideseat-build33-simulator-recovered.log`, and
`/tmp/sideseat-build33-simulator-release-config.log`. The 14 screenshots are
copied to `docs/visual-qa/testflight-33-simulator/` (Git-ignored). The LLDB
version metadata warning also appeared in this successful run; it is not,
by itself, evidence of an application failure.

## External pilot access readiness — 2026-09-08

- Read the signed-in App Store Connect UI for SideSeat, without changing
  access or sending invitations. Build 33 is processed, shows Ready to Submit
  for external testing, and remains assigned only to `SideSeat Internal`
  (one tester, no individual testers).
- An external group already exists: `SideSeat 用户测试`. It has zero testers,
  zero builds and a public invitation link with a displayed 100-tester limit.
  No duplicate group or new link is needed. The link is not currently a
  verified installation route for Build 33.
- Beta feedback email, marketing/privacy URLs, reviewer contact fields and
  review notes are empty; reviewer sign-in is not configured. Contact identity
  and dedicated review credentials must be supplied/authorized by the owner,
  not inferred from other accounts or copied from real users.
- No external Beta App Review was submitted, no testers were added and no
  public-link setting changed. The tester guide now targets Build 33 and
  explicitly gates invitations on actual external availability.
- No production Outcome data was read or written for this access check. The
  Layer 2 measurement contract and next formal snapshot time are unchanged;
  Layer 3 remains blocked.
