/** One row in the chats list — no per-row card; parent <ul> supplies border + dividers. */
export const inboxConversationTileClassName = "list-none";

/** Chats list shell — compact like mainstream chat apps. */
export const inboxChatListUlClassName =
  "divide-y divide-[#ECE8E1] overflow-hidden rounded-xl border border-[#E7E0D6] bg-white dark:divide-border dark:border-border dark:bg-card";
