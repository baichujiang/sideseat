/** One row in the chats list: the parent list supplies only subtle dividers. */
export const inboxConversationTileClassName = "list-none";

/** Chats list shell: edge-to-edge on mobile, with dividers instead of a boxed card. */
export const inboxChatListUlClassName =
  "-mx-3 divide-y divide-[#E7E0D6]/75 bg-transparent dark:divide-border/70 md:mx-0";
